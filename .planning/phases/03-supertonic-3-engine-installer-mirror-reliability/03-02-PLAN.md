---
phase: 03-supertonic-3-engine-installer-mirror-reliability
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - frontend/src-tauri/resources/mirrors.json
  - frontend/src-tauri/tauri.conf.json
  - frontend/src-tauri/src/bootstrap.rs
  - frontend/src-tauri/src/config.rs
  - frontend/src-tauri/Cargo.toml
  - docs/install/restricted-networks.md
  - docs/install/linux.md
  - docs/install/windows.md
  - scripts/verify_mirror_liveness.py
autonomous: false
requirements:
  - INST-07
  - INST-08
  - INST-09
  - INST-10
  - INST-11

must_haves:
  truths:
    - "User in region=china can complete `uv venv` + `uv sync` from a fresh install with no VPN, using mirror cascade — bootstrap reaches `ready` stage"
    - "User in region=restricted whose direct GitHub fetch times out cascades through the mirror list and either succeeds on a mirror or falls back to system Python >=3.11"
    - "If every mirror fails AND no system Python >=3.11 exists on PATH, bootstrap fails with a clear deeplink to docs/install/restricted-networks.md — not with a raw uv error"
    - "`bootstrap.rs` refuses to run `uv sync` without `uv.lock` present (no silent unfrozen resolve)"
    - "Every `uv` invocation in `bootstrap.rs` sets `UV_HTTP_TIMEOUT=120` and `UV_HTTP_RETRIES=5`"
    - "User cannot enter a freeform mirror URL — only region dropdown picks among the JSON allow-list"
    - "Deprecated `UV_INDEX_URL` no longer appears in `bootstrap.rs`; `UV_DEFAULT_INDEX` is set instead (uv 0.4.23+)"
    - "Closing #57 (generic install fail) and #60 (Russian install error) — both bugs report the same root cause and both are fixed by this cascade"
  artifacts:
    - path: "frontend/src-tauri/resources/mirrors.json"
      provides: "Allow-list of github-proxy URLs + per-region PyPI/HF endpoints"
      min_lines: 20
      contains: "python_build_standalone_mirrors"
    - path: "frontend/src-tauri/src/bootstrap.rs"
      provides: "Mirror cascade in ensure_venv_ready: try_mirrors_in_order + probe_system_python_311 + only-system fallback"
      contains: "UV_PYTHON_INSTALL_MIRROR"
    - path: "docs/install/restricted-networks.md"
      provides: "Honest playbook for China/Russia/CIS — VPN guidance, system Python install, mirror rotation"
      min_lines: 80
    - path: "scripts/verify_mirror_liveness.py"
      provides: "curl-style liveness check for every URL in mirrors.json; runs in CI"
      min_lines: 40
      contains: "python_build_standalone_mirrors"
  key_links:
    - from: "frontend/src-tauri/src/bootstrap.rs::ensure_venv_ready"
      to: "frontend/src-tauri/resources/mirrors.json"
      via: "app.path().resource_dir() + serde_json::from_str"
      pattern: "mirrors\\.json"
    - from: "frontend/src-tauri/src/bootstrap.rs"
      to: "frontend/src-tauri/src/config.rs::get_effective_region"
      via: "let region = get_effective_region(app); switch on region"
      pattern: "get_effective_region"
    - from: "frontend/src-tauri/tauri.conf.json (bundle.resources)"
      to: "frontend/src-tauri/resources/mirrors.json"
      via: "resources includes resources/mirrors.json so app.path().resource_dir() finds it"
      pattern: "resources/mirrors\\.json"
    - from: "frontend/src/components/BootstrapSplash.jsx (region dropdown)"
      to: "frontend/src-tauri/src/config.rs::set_region (existing)"
      via: "set_region Tauri command validates against VALID_REGIONS allow-list"
      pattern: "set_region"

threat_model:
  - id: T-03-06
    category: Tampering
    component: mirrors.json contents
    disposition: mitigate
    mitigation: "Shipped read-only in installer bundle (never fetched at runtime); rotation requires signed app release; loaded via app.path().resource_dir() which Tauri verifies against bundle integrity."
  - id: T-03-07
    category: Tampering
    component: PyPI mirror serving tampered wheels
    disposition: mitigate
    mitigation: "INST-10 enforces `uv sync --frozen`; every wheel SHA-256 verified against uv.lock; bootstrap.rs refuses to run uv sync without uv.lock present."
  - id: T-03-08
    category: Spoofing
    component: freeform mirror URL input
    disposition: mitigate
    mitigation: "INST-09 — no UI element accepts freeform mirror URLs; region dropdown VALID_REGIONS allow-list (config.rs:92) is the only entry point; rejected at the Tauri command boundary."
  - id: T-03-09
    category: Denial of Service
    component: all mirrors dead + no system Python
    disposition: mitigate
    mitigation: "Cascade fails with explicit BootstrapStage::Failed + deeplink to docs/install/restricted-networks.md; user gets actionable next step, not a hung process."
  - id: T-03-10
    category: Information Disclosure
    component: bootstrap log emission
    disposition: mitigate
    mitigation: "bootstrap.rs::emit_log redacts HF_TOKEN substrings (verify Phase 1's redactor applies); only-system Python paths are logged but contain no credentials by construction."
---

<objective>
Replace `bootstrap.rs`'s single hard-coded China mirror with a region-aware cascade: try direct GitHub (global), then each mirror in `mirrors.json`, then fall back to system Python >=3.11 via `UV_PYTHON_PREFERENCE=only-system`. Migrate the deprecated `UV_INDEX_URL` (bootstrap.rs:454) to `UV_DEFAULT_INDEX`. Enforce `uv sync --frozen` unconditionally. Set `UV_HTTP_TIMEOUT=120` + `UV_HTTP_RETRIES=5` on every uv invocation.

Purpose: Closes #57 (generic install fail behind restrictive networks) and #60 (Russian install error). REQUIREMENTS.md INST-07..11. Today's bootstrap.rs:454 region check only handles `china` -> Aliyun PyPI (using a deprecated env var) and does NOTHING for Russia, restricted, or python-build-standalone download failures. This plan extends the existing region plumbing — does not invent UI.

Output:
- `frontend/src-tauri/resources/mirrors.json` — allow-list of github-proxy URLs + per-region PyPI/HF endpoints
- `frontend/src-tauri/src/bootstrap.rs` — refactored `ensure_venv_ready` with cascade + only-system fallback + frozen enforcement
- `docs/install/restricted-networks.md` — honest playbook (no Russian government-blessed PyPI mirror per CLAUDE.md Capability 3)
- `scripts/verify_mirror_liveness.py` — CI script that curl-tests each mirror URL; runs in Phase 0's CI matrix to flag drift
- Two existing docs updated (`linux.md`, `windows.md`) with the new env-var sketch
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-supertonic-3-engine-installer-mirror-reliability/03-RESEARCH.md
@frontend/src-tauri/src/bootstrap.rs
@frontend/src-tauri/src/config.rs
@CLAUDE.md

<interfaces>
Existing in-tree contracts that Plan 03-02 EXTENDS but does NOT redesign.

From `frontend/src-tauri/src/config.rs:13-22` (in-tree):
- `pub struct AppConfig { pub region: String, ... }` — serde default `"auto"`.
- `pub fn default_region() -> String { "auto".into() }`
- `pub const VALID_REGIONS: &[&str] = &["auto","global","china","russia","restricted"];`
- `pub fn get_effective_region<R: tauri::Runtime>(app: &tauri::AppHandle<R>) -> String` — resolves `"auto"` via `auto_detect_region()` (probes github.com w/ 4s HEAD).
- `#[tauri::command] pub fn set_region(app: tauri::AppHandle, region: String) -> String` — validates against `VALID_REGIONS`.

From `frontend/src-tauri/src/bootstrap.rs:440-465` (current state to be REPLACED):
- Sets `--frozen` only if `uv.lock` is present (silent unfrozen fallback — INST-10 violation).
- Sets the deprecated `UV_INDEX_URL` to Aliyun only when `effective_region == "china"`.
- No mirror cascade for python-build-standalone.
- No `UV_HTTP_TIMEOUT` / `UV_HTTP_RETRIES`.
- No `only-system` fallback.

uv environment variables (verified against docs.astral.sh/uv/reference/environment/ on 2026-05-18):
- `UV_PYTHON_INSTALL_MIRROR` — prefix-replaces `https://github.com/astral-sh/python-build-standalone/releases/download`.
- `UV_PYTHON_PREFERENCE` — `managed` (default), `only-managed`, `system`, `only-system`.
- `UV_DEFAULT_INDEX` — replaces deprecated `UV_INDEX_URL` (uv 0.4.23+).
- `UV_HTTP_TIMEOUT` — seconds, default 30.
- `UV_HTTP_RETRIES` — default 3 (uv 0.7.21+).
- `UV_HTTP_CONNECT_TIMEOUT` — seconds, default 10.

Target shape for `resources/mirrors.json` (per 03-RESEARCH.md Code Examples):

  {
    "python_build_standalone_mirrors": [
      "https://ghproxy.net/https://github.com/astral-sh/python-build-standalone/releases/download",
      "https://ghfast.top/https://github.com/astral-sh/python-build-standalone/releases/download",
      "https://gitmirror.com/https://github.com/astral-sh/python-build-standalone/releases/download"
    ],
    "pypi_default_index": {
      "china":      "https://mirrors.aliyun.com/pypi/simple/",
      "russia":     "https://pypi.org/simple/",
      "restricted": "https://pypi.org/simple/",
      "global":     "https://pypi.org/simple/"
    },
    "hf_endpoint": {
      "china":      "https://hf-mirror.com",
      "russia":     "https://huggingface.co",
      "restricted": "https://huggingface.co",
      "global":     "https://huggingface.co"
    }
  }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify mirror liveness, ship mirrors.json, wire Tauri bundle resource</name>
  <files>scripts/verify_mirror_liveness.py, frontend/src-tauri/resources/mirrors.json, frontend/src-tauri/tauri.conf.json</files>
  <action>
    Per Open Q4 in 03-RESEARCH.md, the three github-proxy mirror URLs (ghproxy.net, ghfast.top, gitmirror.com) need plan-time liveness verification before they go into a shipped allow-list. Mirror drift is documented in dautovri/mirrors-china as a known hazard.

    1. Write `scripts/verify_mirror_liveness.py`:
       - Reads `frontend/src-tauri/resources/mirrors.json` (or accepts `--file` arg).
       - For each URL in `python_build_standalone_mirrors`, performs HTTPS HEAD to `{mirror}/20240909/cpython-3.11.10+20240909-x86_64-unknown-linux-gnu-install_only.tar.gz` (a known-existing python-build-standalone artifact path — pick a stable release tag that has been live >=6 months). Reports `OK 200`, `FAIL 4xx/5xx`, or `TIMEOUT` per URL.
       - For each URL in `pypi_default_index` values, performs HTTPS HEAD to `{url}` (or `{url}simple/` for `simple/`-style endpoints). Reports same.
       - For each URL in `hf_endpoint` values, performs HTTPS HEAD to `{url}/api/models`. Reports same.
       - Exits 0 if every URL responded with 2xx/3xx within `UV_HTTP_CONNECT_TIMEOUT=30s`; exits 1 with a tabular summary otherwise.
       - Uses `httpx` (already in dev deps per pyproject.toml — verify with `grep httpx pyproject.toml`).

    2. Run `python scripts/verify_mirror_liveness.py --file /dev/stdin` against an in-memory draft `mirrors.json` that contains the three github-proxy URLs from 03-RESEARCH.md Code Examples. If any return 4xx/5xx, REMOVE that URL from the draft before writing to disk. Document removed mirrors in a JSON-internal comment key (`_dead_mirrors_2026_05_19`). Per Open Q4 resolution.

    3. Write `frontend/src-tauri/resources/mirrors.json` with the surviving allow-list using the schema shown in the `<interfaces>` block above. Important details:
       - Include a leading `_comment` key documenting "INST-09 forbids freeform mirror input; rotate via signed app release; liveness last verified: 2026-05-19 via scripts/verify_mirror_liveness.py."
       - Russia gets `pypi.org` direct + `huggingface.co` direct because no government-blessed mirror exists (CLAUDE.md Capability 3 explicit decision; documented honestly in Task 3's docs).
       - China gets `mirrors.aliyun.com/pypi/simple/` + `hf-mirror.com` (already verified live in 03-RESEARCH.md Environment Availability).

    4. Edit `frontend/src-tauri/tauri.conf.json` — add `"resources/mirrors.json"` to the `bundle.resources` array (or `tauri.bundle.resources`, depending on Tauri 2 schema in tree; cross-reference the existing `tauri.conf.json` for the right field path). This makes `app.path().resource_dir().join("mirrors.json")` resolve at runtime on macOS / Windows / Linux per A4 in 03-RESEARCH.md Assumptions Log.

    5. Add the verify script to CI via `scripts/smoke-test.sh` (already in tree per Phase 0 GATE-03). One-line append:
       `python scripts/verify_mirror_liveness.py || echo "::warning::mirror liveness check failed (informational, does not block CI)"`
       Do not hard-fail CI on transient mirror downtime — log a warning instead.
  </action>
  <verify>
    <automated>python scripts/verify_mirror_liveness.py &amp;&amp; python -c "import json; m=json.load(open('frontend/src-tauri/resources/mirrors.json')); assert len(m['python_build_standalone_mirrors'])&gt;=1; assert set(m['pypi_default_index'].keys())=={'china','russia','restricted','global'}; assert set(m['hf_endpoint'].keys())=={'china','russia','restricted','global'}; print('mirrors.json OK')"</automated>
  </verify>
  <done>mirrors.json shipped in bundle.resources; at least one live python-build-standalone mirror present; all four regions covered for PyPI + HF endpoints; verify script wired into CI as a warning step.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor bootstrap.rs — mirror cascade + only-system fallback + frozen enforcement + uv env vars</name>
  <files>frontend/src-tauri/src/bootstrap.rs, frontend/src-tauri/src/config.rs, frontend/src-tauri/Cargo.toml</files>
  <action>
    Implement the mirror cascade per Pattern 3 in 03-RESEARCH.md. This task is the bulk of Wave 2.

    1. Add a serde struct + loader in `bootstrap.rs`:
       - `struct MirrorList { python_build_standalone_mirrors: Vec<String>, pypi_default_index: HashMap<String,String>, hf_endpoint: HashMap<String,String> }` with `#[derive(serde::Deserialize, Debug)]`.
       - `fn load_mirror_list<R: tauri::Runtime>(app: &tauri::AppHandle<R>) -> Option<MirrorList>` — resolves `app.path().resource_dir()?.join("mirrors.json")`, reads, `serde_json::from_str`, returns `None` on any error (caller treats `None` as empty cascade -> direct + only-system fallback).
       - Confirm `Cargo.toml` already depends on `serde_json`. Search; it almost certainly does (config.rs uses serde). Add if missing.

    2. Implement `fn probe_system_python_311() -> Option<PathBuf>` per Pattern 3 + Pitfall 3 in 03-RESEARCH.md. The probe runs `<candidate> -c "import sys; sys.exit(0 if sys.version_info >= (3, 11) else 1)"` and on success uses `which` (Unix) or `where` (Windows) to resolve the binary path. Windows candidates: `py`, `python3.11`, `python3`, `python`. Unix candidates: `python3.13`, `python3.12`, `python3.11`, `python3`. Prefer newer versions when multiple are present (order the list newest-first).

    3. Implement `fn run_uv_venv_with_cascade<R: tauri::Runtime>(app: &tauri::AppHandle<R>, uv_path: &Path, project_dir: &Path, region: &str) -> Result<(), String>` per Pattern 3 in 03-RESEARCH.md:
       - Always set common env on every attempt: `UV_HTTP_TIMEOUT=120`, `UV_HTTP_CONNECT_TIMEOUT=30`, `UV_HTTP_RETRIES=5` (INST-11).
       - Build attempt list: for `region == "global"`, start with `None` (direct GitHub), then each mirror in order. For other regions, skip direct and go straight to mirrors.
       - For each attempt: set `UV_PYTHON_INSTALL_MIRROR=mirror` (skip on `None`), run `uv venv --python 3.11 --managed-python` via `run_streaming(app, "creating_venv", &mut cmd)`, break on success, log warn + continue on failure.
       - On exhaustion: probe system Python 3.11+; on found, re-run `uv venv --python <path> --python-preference only-system`; on not-found, return `Err` with a message that includes the deeplink to `docs/install/restricted-networks.md` and the names of mirrors attempted. The caller wraps with `fail(progress, &msg)`.

    4. Replace the existing `bootstrap.rs:441-455` block. The `has_lockfile` branch becomes a hard gate (INST-10):
       - If `!has_lockfile`, call `fail(progress, "uv.lock missing from bundle — refusing to install without hash verification. Reinstall OmniVoice from a signed release artifact.")` and return `None`.
       - The `else` branch that ran `uv sync` without `--frozen` is REMOVED. There is no silent unfrozen fallback.

    5. Replace the `effective_region == "china"` block (current bootstrap.rs:454) with region-data-driven env setup:
       - `let mirrors = load_mirror_list(app);`
       - Apply common uv HTTP env (INST-11) to BOTH the `uv venv` cascade and the `uv sync` command: `UV_HTTP_TIMEOUT=120`, `UV_HTTP_CONNECT_TIMEOUT=30`, `UV_HTTP_RETRIES=5`.
       - PyPI mirror — replaces deprecated `UV_INDEX_URL` with `UV_DEFAULT_INDEX` (uv 0.4.23+). If `mirrors.pypi_default_index` has an entry for `effective_region`, set `UV_DEFAULT_INDEX=<that url>` on `sync_cmd`.
       - Confirm zero remaining usages of `UV_INDEX_URL` in the file via `grep -n UV_INDEX_URL frontend/src-tauri/src/bootstrap.rs` — must return empty.

    6. Invoke the cascade BEFORE the existing `uv venv` call. The cascade REPLACES that call (and the existing `--managed-python` invocation). Wire so that `ensure_venv_ready` calls `run_uv_venv_with_cascade(...)` and on `Err` returns `None` via `fail()`.

    7. Add Rust unit tests in `frontend/src-tauri/src/bootstrap.rs` under `#[cfg(test)] mod tests`:
       - `test_mirrors_json_loadable` (INST-08): Constructs a `MirrorList` via `serde_json::from_str` against a known-good JSON literal; asserts deserialization succeeds and the three top-level keys round-trip.
       - `test_cascade_skips_failed` (INST-07): Mocks two mirrors where the first returns failure; asserts the second is attempted. If `run_streaming` is hard to inject, isolate the cascade-iteration policy into a pure helper `fn next_attempt(history: &[bool]) -> Option<usize>` and test that helper directly.
       - `test_frozen_required` (INST-10): Construct a temp dir without `uv.lock`; invoke the `has_lockfile`-check branch; assert the failure path is taken (test the helper that returns the fail-message, not the full Tauri-app flow).
       - `test_http_env_set` (INST-11): Constructs a `std::process::Command` via the same code path as `sync_cmd` setup; reads its env via `Command::get_envs()`; asserts `UV_HTTP_TIMEOUT=120` and `UV_HTTP_RETRIES=5` present.
       - `test_python_probe_finds_311`: On a host that has `python3.11+`, `probe_system_python_311()` returns `Some(_)`. Gate with `#[ignore]` so CI runners without python3.11 skip it.

    8. Add a config.rs test for the region allow-list (INST-09):
       - `test_rejects_unknown_region`: asserts `VALID_REGIONS` does NOT contain freeform URL-like strings (`"freeform-url"`, `"http://evil.example.com"`).
       - `test_no_freeform_mirror_command_exported`: `include_str!("config.rs")` and asserts it does not contain the substring `"set_mirror_url"` — guards against accidental introduction of a freeform-mirror Tauri command.

    Do NOT touch `backend.rs::spawn_backend`'s `HF_ENDPOINT=https://hf-mirror.com` block (already in tree at lines 168-173) — that path remains unchanged. The `hf_endpoint` map in `mirrors.json` is informational/documentation for Phase 3; future plans may wire it into `spawn_backend` to make HF endpoint data-driven, but that is OUT OF SCOPE for Phase 3 per the Deferred Ideas list in 03-RESEARCH.md.
  </action>
  <verify>
    <automated>cd /Users/user4/Desktop/voice-design/OmniVoice/frontend/src-tauri &amp;&amp; cargo build --release 2&gt;&amp;1 | grep -E 'error\[|warning:' | head -20 ; cd /Users/user4/Desktop/voice-design/OmniVoice/frontend/src-tauri &amp;&amp; cargo test bootstrap config 2&gt;&amp;1 | tail -30 ; test "$(grep -c UV_INDEX_URL /Users/user4/Desktop/voice-design/OmniVoice/frontend/src-tauri/src/bootstrap.rs)" = "0"</automated>
  </verify>
  <done>`cargo build --release` clean (no new errors / no new warnings other than dead-code in test gates); `cargo test bootstrap config` green; `grep -c UV_INDEX_URL bootstrap.rs` returns 0; mirror cascade reachable from `ensure_venv_ready`.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Write restricted-networks playbook + update linux/windows install docs + manual cross-platform smoke</name>
  <files>docs/install/restricted-networks.md, docs/install/linux.md, docs/install/windows.md</files>
  <action>
    Wave 2 hardened `bootstrap.rs` against restricted networks. The bootstrap-failure UI (Phase 1 Plan 01-02 error-deeplink map) and the docs the deeplink points to are the user-facing half of #57 and #60 closure. This task writes those docs and then manually exercises the cascade on each target OS — there is no other way to prove the cascade actually closes #57/#60 except to run a fresh install on a restricted-network VM image.

    First write the docs (the docs are short prose, not a separate auto task):

    1. Create `docs/install/restricted-networks.md` (~80-150 lines, GitHub-rendered Markdown):
       - Section "What this page covers": list the three failure modes Phase 3 addresses — slow/blocked github.com (python-build-standalone), slow/blocked pypi.org, slow/blocked huggingface.co.
       - Section "Quick fix — pick your region": describe the BootstrapSplash region dropdown (auto / global / china / russia / restricted). Explain auto-detect probes github.com w/ a 4-second HEAD; if it fails, region drops to `restricted` automatically.
       - Section "China-specific": cascade goes through ghproxy.net / ghfast.top / gitmirror.com for python-build-standalone, mirrors.aliyun.com for PyPI, hf-mirror.com (padeoe public-welfare project) for HF model weights. All HTTPS, all read-only. List the URLs verbatim from `mirrors.json` so docs and shipped allow-list match.
       - Section "Russia / CIS": HONEST callout — no government-blessed PyPI or HF mirror exists. Two viable paths:
         (a) Install Python 3.11+ from python.org (link to the Windows installer download and apt/dnf snippets for Debian/Ubuntu/Fedora); OmniVoice's bootstrap falls back to `UV_PYTHON_PREFERENCE=only-system` and uses that.
         (b) Use a VPN; switch region to `global`.
       - Section "Everywhere else": region=restricted uses pypi.org direct + huggingface.co direct, but with `UV_HTTP_TIMEOUT=120` + 5 retries.
       - Section "If all mirrors fail": the bootstrap UI shows a specific failure page with a "Retry" button and a deeplink back to this page. Document what to check (firewall, corporate proxy, MITM cert intercept).
       - Section "Reporting a mirror as dead": link to GitHub issue template; explain that mirrors are rotated via signed app updates (no runtime mutation).
       - Section "Advanced: edit config.json directly": document the `region` field in `omnivoice_data/config.json` for the advanced-user override path (acknowledged in 03-RESEARCH.md Claude's Discretion).

    2. Update `docs/install/linux.md` — append a "Restricted networks" subsection: "If `bootstrap` fails to download Python, see `restricted-networks.md`. The most common fix is `sudo apt install python3.11` then restart the app." Cross-link.

    3. Update `docs/install/windows.md` — append the same subsection for Windows. The most common fix is `winget install Python.Python.3.11` then restart the app. Cross-link.

    Then run the manual cross-platform smoke (this is the work the human checkpoint actually gates on — Phase 3 must not ship without it):

    4. macOS test (UTM or local): Set `region = china` in `omnivoice_data/config.json` BEFORE first launch. Boot the app. Watch BootstrapSplash. Confirm:
       - Log line `Trying python-build-standalone mirror: https://ghproxy.net/...` appears.
       - Either that mirror succeeds, or cascade proceeds to ghfast / gitmirror, or only-system fallback fires.
       - Bootstrap reaches `ready` stage. Backend logs do not contain `UV_INDEX_URL` (only `UV_DEFAULT_INDEX`).

    5. Windows test (Hyper-V or local): Same as macOS but with `region = restricted` AND temporarily block `github.com` via `hosts` file (`127.0.0.1 github.com`). Confirm cascade fails through to either a working mirror or only-system Python (if python3.11+ is on PATH). If neither works, confirm bootstrap shows the deeplink to `docs/install/restricted-networks.md`. Restore hosts file after.

    6. Linux test (Ubuntu 24.04 VM): `region = russia`. Confirm:
       - Cascade tries mirrors first (not direct GitHub).
       - If all mirrors fail (simulate with `iptables OUTPUT -d github.com -j DROP`), the `only-system` fallback finds `python3.11+` (Ubuntu 24.04 has it) and bootstrap completes.

    7. #57 verification: Reproduce the original #57 stack trace path. With the new cascade, the app completes bootstrap. Capture the new log output for the release notes.

    8. #60 verification: Same for #60 (Russian install error). Capture log output.

    Resume signals:
    - All four OS smokes pass AND #57/#60 reproduce-to-fix is captured -> type "approved" with brief notes on each OS test.
    - Any smoke fails -> type "blocked: &lt;reason&gt;" with log excerpts attached; the cascade needs fixing before this plan closes.
  </action>
  <verify>
    <human-check>
      Three new/modified docs files exist (`docs/install/restricted-networks.md` ~80+ lines, `linux.md` + `windows.md` updated with cross-links). macOS / Windows / Linux smoke runs each demonstrate the cascade working on the configured region. #57 and #60 each have a captured log excerpt showing the new cascade succeeds where the old code failed.
    </human-check>
  </verify>
  <done>Resume signal "approved" received with per-OS notes; docs committed; #57/#60 log excerpts saved to phase notes folder for the eventual release-notes verification line (REL-03).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Installer bundle -> runtime | `mirrors.json` crosses here once at app install; never re-fetched at runtime. Bundle integrity is Tauri's responsibility. |
| Region selection (UI) -> bootstrap (Rust) | `set_region` Tauri command validates against `VALID_REGIONS` allow-list (config.rs:142). No freeform input crosses. |
| uv subprocess -> mirror URL | HTTPS-only by JSON allow-list. Wheel SHA-256 verified against `uv.lock` (INST-10). |
| only-system Python on PATH -> bootstrap | Probed binary executes on the user's machine with the user's privileges. Trust is delegated to the OS PATH. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Tampering | mirrors.json contents | mitigate | Shipped read-only in installer bundle; rotation via signed app release only |
| T-03-07 | Tampering | PyPI mirror serves tampered wheel | mitigate | `uv sync --frozen` enforced (INST-10); SHA-256 in uv.lock fails the install on bytewise mismatch |
| T-03-08 | Spoofing | freeform mirror URL input | mitigate | INST-09 — `VALID_REGIONS` allow-list at the Tauri command boundary; Task 2 test asserts no `set_mirror_url` command exists |
| T-03-09 | Denial of Service | all mirrors dead + no system Python | mitigate | Hard fail with deeplink to docs/install/restricted-networks.md; not a hung process |
| T-03-10 | Information Disclosure | bootstrap log emission | mitigate | Phase 1's logging redactor applies to bootstrap.rs::emit_log; only-system Python paths logged contain no credentials by construction |
</threat_model>

<verification>
- `cd frontend/src-tauri && cargo build --release` clean (no new errors).
- `cd frontend/src-tauri && cargo test bootstrap config` green.
- `grep -c UV_INDEX_URL frontend/src-tauri/src/bootstrap.rs` returns 0 (deprecated env var fully removed).
- `python scripts/verify_mirror_liveness.py` succeeds against the shipped `mirrors.json`.
- Manual cross-platform smoke (Task 3 human-verify) confirms cascade actually closes #57 and #60 on at least three OSes (macOS, Windows, Linux).
- `bootstrap.rs` emits no log lines containing `hf_*` token substrings during a full install (Phase 1's redactor verified to apply).
</verification>

<success_criteria>
- INST-07: Cascade reaches a working venv on a restricted-network machine via mirrors OR only-system fallback; failure path emits a deeplink, not a raw uv error.
- INST-08: `mirrors.json` is rotatable without a release-binary patch (the file is in `bundle.resources`; rotating requires a signed release but no code change).
- INST-09: No UI or Tauri command accepts freeform mirror URLs; `VALID_REGIONS` is the only entry point; config.rs test asserts this.
- INST-10: `uv sync --frozen` is unconditional; missing `uv.lock` is a hard failure; cargo test `test_frozen_required` passes.
- INST-11: `UV_HTTP_TIMEOUT=120` and `UV_HTTP_RETRIES=5` set on every uv invocation; cargo test `test_http_env_set` passes.
- #57 and #60: Manual smoke (Task 3) shows the bootstrap completes on at least one previously-failing network condition for each issue, with log excerpts captured for release notes.
</success_criteria>

<output>
Create `.planning/phases/03-supertonic-3-engine-installer-mirror-reliability/03-02-SUMMARY.md` when done.
</output>
