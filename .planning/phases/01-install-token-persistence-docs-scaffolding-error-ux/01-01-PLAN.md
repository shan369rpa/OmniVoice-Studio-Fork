---
phase: 01-install-token-persistence-docs-scaffolding-error-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/services/token_resolver.py
  - backend/services/settings_store.py
  - backend/core/logging_filter.py
  - backend/core/db.py
  - backend/migrations/versions/0001_phase1_settings_table.py
  - backend/api/routers/dub_core.py
  - backend/api/routers/system.py
  - backend/services/sonitranslate.py
  - backend/services/model_manager.py
  - backend/api/routers/settings.py
  - backend/main.py
  - tests/backend/services/test_token_resolver.py
  - tests/backend/services/test_settings_store.py
  - tests/backend/core/test_logging_filter.py
  - tests/backend/test_engine_spawn_token.py
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-04
  - AUTH-05
  - AUTH-06

must_haves:
  truths:
    - "User who runs `huggingface-cli login` and restarts OmniVoice sees diarization gate clear without setting HF_TOKEN env var"
    - "User who saves an HF token in Settings → API Keys has it survive app restart and reach IndexTTS/pyannote subprocesses"
    - "HF token stored in SQLite is encrypted at rest (not plaintext on inspection)"
    - "No `hf_*` token string appears in any log file, error toast, or traceback"
    - "When the active token returns HTTP 401 mid-download, resolver auto-retries the next source"
    - "Existing `omnivoice_data/` SQLite from v0.2.7 keeps working after upgrade with no manual migration"
  artifacts:
    - path: "backend/services/token_resolver.py"
      provides: "3-source resolver (App → Env → HF CLI) with on_401 fallback"
      min_lines: 80
      contains: "def resolve"
    - path: "backend/services/settings_store.py"
      provides: "Encrypted settings table CRUD (get/set/clear hf_token)"
      min_lines: 60
      contains: "Fernet"
    - path: "backend/core/logging_filter.py"
      provides: "HFTokenRedactor logging.Filter subclass"
      min_lines: 25
      contains: "HFTokenRedactor"
    - path: "backend/migrations/versions/0001_phase1_settings_table.py"
      provides: "Alembic migration adding settings(key, value, updated_at) table"
      contains: "op.create_table"
    - path: "tests/backend/services/test_token_resolver.py"
      provides: "Unit tests for AUTH-01, AUTH-03, AUTH-06 cascade + 401 fallback"
      min_lines: 80
    - path: "tests/backend/services/test_settings_store.py"
      provides: "Round-trip encryption test for AUTH-02"
      min_lines: 30
    - path: "tests/backend/core/test_logging_filter.py"
      provides: "Redaction test asserting `hf_*` never reaches handlers"
      min_lines: 25
  key_links:
    - from: "backend/api/routers/dub_core.py:540"
      to: "backend/services/token_resolver.py"
      via: "import token_resolver; resolved = token_resolver.resolve()"
      pattern: "token_resolver\\.resolve"
    - from: "backend/services/sonitranslate.py"
      to: "backend/services/token_resolver.py"
      via: "replace bare os.environ.get('HF_TOKEN') with resolver"
      pattern: "token_resolver\\.resolve"
    - from: "backend/services/model_manager.py"
      to: "backend/services/token_resolver.py"
      via: "replace bare os.environ.get('HF_TOKEN') with resolver"
      pattern: "token_resolver\\.resolve"
    - from: "backend/main.py (startup)"
      to: "backend/core/logging_filter.py"
      via: "logging.getLogger().addFilter(HFTokenRedactor())"
      pattern: "addFilter.*HFTokenRedactor"
    - from: "subprocess engine launchers"
      to: "token_resolver.resolve()"
      via: "env['HF_TOKEN'] = resolved.token before Popen"
      pattern: "env\\[.HF_TOKEN.\\]\\s*="

threat_model:
  - id: T-01-01
    category: Information Disclosure
    component: backend/services/settings_store.py
    disposition: mitigate
    mitigation: "Token column encrypted with Fernet; key derived via scrypt from OS machine-id + per-install random salt stored in same settings table"
  - id: T-01-02
    category: Information Disclosure
    component: backend/core/logging_filter.py
    disposition: mitigate
    mitigation: "HFTokenRedactor logging filter strips `hf_[A-Za-z0-9]{30,}` from record.msg and record.args at root logger; CI asserts redaction via caplog"
  - id: T-01-03
    category: Spoofing
    component: backend/api/routers/settings.py (new save_hf_token endpoint)
    disposition: mitigate
    mitigation: "Apply same loopback origin check shipped in quick task 260518-ivy (commit e1f08a6) to all new token-write endpoints; reject non-loopback Origin/Host headers"
  - id: T-01-04
    category: Information Disclosure
    component: subprocess engine env injection (AUTH-04)
    disposition: accept
    mitigation: "Child engines need HF_TOKEN in env to authenticate downloads; risk is bounded to user's own process tree on same machine"
---

<objective>
Land the 3-source HF token cascade (App → Env → HF CLI), encrypted SQLite storage, subprocess env injection, and the logging-redaction filter. Patch every bare `os.environ.get("HF_TOKEN")` call site in the backend to use the resolver. Closes #35 and its diarization sub-issue. Backward-compatible with users who already ran `huggingface-cli login` or who already have `HF_TOKEN` exported.

Purpose: A user enters their HF token ONCE — anywhere (Settings, shell rc, or `huggingface-cli login`) — and OmniVoice finds it. The current #35 bug (bare `os.environ` check in `dub_core.py:540`) makes `huggingface-cli login` users see "no HF_TOKEN" even though the library would have read it. This plan eliminates that class of bug systemically.

Output: Resolver service + settings store + log filter + migration + tests + all read-side call sites patched. Settings → API Keys UI panel ships in Plan 01-02 (which depends on this plan's resolver API). Note: `backend/core/links.py` (project repo URL constant for docs deeplinks) is owned by Plan 01-02 — this plan does NOT create it (per checker B-6 resolution).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-install-token-persistence-docs-scaffolding-error-ux/01-RESEARCH.md
@.planning/phases/01-install-token-persistence-docs-scaffolding-error-ux/01-VALIDATION.md

@CLAUDE.md
@backend/api/routers/dub_core.py
@backend/core/db.py
@backend/services/sonitranslate.py
@backend/services/model_manager.py
@backend/api/routers/system.py

<interfaces>
<!-- Resolver contract that this plan creates and Plan 01-02 + 01-03 consume. -->

```python
# backend/services/token_resolver.py — public API
from dataclasses import dataclass
from typing import Literal, Optional

Source = Literal["app", "env", "hf-cli"]

@dataclass(frozen=True)
class ResolvedToken:
    token: str
    source: Source
    username: Optional[str]   # whoami username if validated, else None

@dataclass(frozen=True)
class SourceState:
    source: Source
    set: bool
    masked: Optional[str]      # "hf_…3jw" or None
    whoami_user: Optional[str]
    whoami_ok: bool

def resolve(skip: frozenset[Source] = frozenset()) -> Optional[ResolvedToken]:
    """Return highest-priority valid token, or None if all sources empty/invalid."""

def on_401(active_source: Source) -> Optional[ResolvedToken]:
    """AUTH-06: retry resolution skipping the source that just 401'd."""

def state() -> dict:
    """Returns {'sources': list[SourceState], 'active': Optional[Source]} for the Settings UI."""

def save_app_token(token: str) -> None:
    """Save to SQLite settings_store + populate ~/.cache/huggingface/token via huggingface_hub.login()."""

def clear_app_token(also_clear_hf_cli: bool = False) -> None:
    """Remove from app store; optionally call huggingface_hub.logout() too."""
```

```python
# backend/services/settings_store.py — public API
def get_hf_token() -> Optional[str]: ...
def set_hf_token(token: str) -> None: ...
def clear_hf_token() -> None: ...
```

```python
# backend/core/logging_filter.py — public API
class HFTokenRedactor(logging.Filter):
    REDACTED = "hf_***REDACTED***"
    def filter(self, record: logging.LogRecord) -> bool: ...

def install_redaction_filter(root_logger=None) -> None: ...
```
</interfaces>

<existing_state>
- `backend/core/db.py` runs `_BASE_SCHEMA` (CREATE TABLE IF NOT EXISTS) and exposes `db_conn()`. It does NOT have a `settings` table today.
- `alembic.ini` exists; migration script_location is `backend/migrations`. AUTH-02 MUST add a real migration under `backend/migrations/versions/`.
- `pyproject.toml [project.urls].Repository = "https://github.com/k2-fsa/OmniVoice"` exists; this is the upstream model repo, NOT the OmniVoice Studio Tauri app. Tauri identifier is `com.debpalash.omnivoice-studio` and updater URL is `github.com/debpalash/OmniVoice-Studio`. For docs deeplinks the OmniVoice Studio fork is the right target. **Per checker B-6 resolution: `backend/core/links.py` (the single source of truth for the project repo URL) is owned by Plan 01-02, where the docs-deeplink consumers live. This plan does NOT create it.**
- HF_TOKEN read sites confirmed by grep this session:
  - `backend/api/routers/dub_core.py:540`
  - `backend/api/routers/system.py:32`
  - `backend/services/sonitranslate.py:143, 217`
  - `backend/services/model_manager.py:480`
  All five must be patched to use the resolver.
- `cryptography` availability: assumed transitive via `pyannote-audio`/`huggingface_hub` chain (Assumption A1 in RESEARCH.md). Verify via `uv pip list | grep cryptography` in Task 1; if absent, add to `[project.dependencies]`.
- Loopback origin check pattern shipped 2026-05-18 in commit `e1f08a6` on `/system/set-env`. Reuse same helper for the new settings endpoint (AUTH-03 backend half).
</existing_state>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Settings store + alembic migration + cryptography sanity</name>
  <files>backend/services/settings_store.py, backend/core/db.py, backend/migrations/versions/0001_phase1_settings_table.py, backend/services/_secret_key.py, tests/backend/services/test_settings_store.py</files>
  <behavior>
    - `settings_store.set_hf_token("hf_abc...")` then `settings_store.get_hf_token()` round-trips to the exact same string
    - The raw bytes stored in the SQLite `settings.value` column do NOT contain the substring `hf_` — encrypted, not plaintext
    - `settings_store.clear_hf_token()` followed by `get_hf_token()` returns `None`
    - `set_hf_token` writes both the encrypted token row AND, on first write, generates and stores the per-install salt row (`_secret_key` row) if absent
    - Alembic upgrade head on an existing v0.2.7 `omnivoice_data/omnivoice.db` (simulated via fixture: pre-Phase-1 schema = `voice_profiles` + `generation_history` + `dub_history` + `studio_projects` + `export_history` + `glossary_terms` + `jobs` + `job_events` only) succeeds and leaves all existing tables untouched, only adding `settings`
    - Alembic downgrade -1 drops the `settings` table cleanly
    - Concurrent reads from two threads return consistent values (sqlite WAL + `db_conn` is already configured)
  </behavior>
  <action>
    Step 1 (sanity, not code-producing): Run `uv pip list | grep -i cryptography` to confirm Fernet availability. If absent, add `cryptography>=41` to `pyproject.toml [project.dependencies]` and run `uv lock --upgrade-package cryptography`. Per Assumption A1 in RESEARCH.md.

    Step 2: Create `backend/services/_secret_key.py` with a single function `derive_fernet_key() -> bytes`:
      - Read or create a 16-byte random salt stored as `settings` row `_secret_key_salt` (base64-encoded). On first call, generate via `os.urandom(16)` and write atomically.
      - Read OS machine identifier:
        - macOS: parse `ioreg -rd1 -c IOPlatformExpertDevice` for `IOPlatformUUID`
        - Linux: read `/etc/machine-id` (fall back to `/var/lib/dbus/machine-id`)
        - Windows: read `HKLM\SOFTWARE\Microsoft\Cryptography` registry value `MachineGuid` via `winreg`
        - Fallback if all three fail: derive from `socket.gethostname() + getpass.getuser()` (documented limitation; warn-log once)
      - Use `cryptography.hazmat.primitives.kdf.scrypt.Scrypt(salt=salt, length=32, n=2**14, r=8, p=1).derive(machine_id_bytes)` then `base64.urlsafe_b64encode` to produce the Fernet key.
      - Cache the derived key in module-level variable; expose `invalidate()` for tests.

    Step 3: Create `backend/services/settings_store.py`:
      - Public API exactly as declared in `<interfaces>` above.
      - Storage: SQLite `settings` table `(key TEXT PRIMARY KEY, value TEXT NOT NULL, updated_at REAL NOT NULL)`.
      - `set_hf_token(token)`: encrypt via `Fernet(derive_fernet_key()).encrypt(token.encode())`, UPSERT into row keyed `hf_token`.
      - `get_hf_token()`: SELECT row keyed `hf_token`; if absent return None. Decrypt via Fernet; on `InvalidToken` (machine-id changed, e.g. user migrated `omnivoice_data/` across machines per Open Question #5), log a warning and return None — caller falls through to env/HF-CLI sources naturally.
      - `clear_hf_token()`: DELETE row keyed `hf_token`. Do NOT clear the salt row.
      - Use `db_conn()` from `backend/core/db.py`.

    Step 4: Patch `backend/core/db.py` `_BASE_SCHEMA` to include the `settings` table definition guarded by `CREATE TABLE IF NOT EXISTS` so fresh installs work without running alembic. Alembic migration in Step 5 is for upgrades from v0.2.7 to v0.3.0 where `_BASE_SCHEMA` may have already executed without the settings table on a prior run — alembic handles the explicit upgrade path. Both paths converge on the same schema.

    Step 5: Create `backend/migrations/versions/0001_phase1_settings_table.py` (use `alembic revision -m "Phase 1: settings table"` to scaffold then edit):
      - `upgrade()`: `op.create_table('settings', sa.Column('key', sa.Text, primary_key=True), sa.Column('value', sa.Text, nullable=False), sa.Column('updated_at', sa.REAL, nullable=False))`
      - `downgrade()`: `op.drop_table('settings')`
      - `revision = '0001_phase1_settings'`, `down_revision = None` if this is the first migration in the project, otherwise chain to the current head (check `alembic current` output during the task).

    Step 6: Write `tests/backend/services/test_settings_store.py` covering the behaviors above. Use a temp DB via monkeypatching `core.config.DB_PATH` to a `tmp_path / 'test.db'`. Include an alembic upgrade test that creates a fixture pre-Phase-1 DB (manually CREATE TABLE the v0.2.7 tables), runs `alembic upgrade head` programmatically via `alembic.config.Config` + `alembic.command.upgrade`, and asserts the existing tables still have their original rows and a new `settings` table exists.

    Implementation per D-locked decisions from research: Fernet (not raw AEAD) per Don't Hand-Roll section; scrypt key derivation per Pitfall #5; no plaintext storage per AUTH-02; alembic real migration (not `init_db()` patch) per Open Question #2 resolution given that the project already has `alembic.ini` and `backend/migrations/`.
  </action>
  <verify>
    <automated>uv run pytest tests/backend/services/test_settings_store.py -x -v</automated>
  </verify>
  <done>
    Settings store round-trips encrypted tokens, alembic upgrade applies cleanly to a v0.2.7 fixture DB without data loss, alembic downgrade -1 reverses the change, `pytest tests/backend/services/test_settings_store.py` is green, and `uv pip list | grep cryptography` confirms the dep is present.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Token resolver + logging filter + read-side call sites</name>
  <files>backend/services/token_resolver.py, backend/core/logging_filter.py, backend/main.py, backend/api/routers/dub_core.py, backend/api/routers/system.py, backend/services/sonitranslate.py, backend/services/model_manager.py, tests/backend/services/test_token_resolver.py, tests/backend/core/test_logging_filter.py</files>
  <behavior>
    `test_token_resolver.py`:
      - `test_resolve_priority`: App set + Env set + HF-CLI set → resolver returns App.
      - `test_resolve_skips_empty`: App unset + Env set + HF-CLI unset → resolver returns Env.
      - `test_resolve_returns_none_when_all_empty`: All three unset → resolver returns None.
      - `test_resolve_skips_401`: App set but `whoami` returns `HfHubHTTPError(401)` + Env set valid → resolver returns Env, not App.
      - `test_on_401_cascade`: Active source = "app", call `on_401("app")` → returns next valid source (env or hf-cli) skipping "app".
      - `test_state_returns_three_rows`: `state()` returns exactly 3 SourceState entries in priority order with `set`/`masked`/`whoami_*` populated.
      - `test_save_writes_both`: `save_app_token("hf_xxx")` writes to `settings_store` AND calls `huggingface_hub.login(token="hf_xxx", add_to_git_credential=False)`.
      - `test_save_uses_add_to_git_credential_false`: confirms the kwarg (Pitfall #2).
    `test_logging_filter.py`:
      - `test_redacts_msg`: logger emits `"download failed for hf_abcdef0123456789012345678901234567890"` → caplog record's final text contains `hf_***REDACTED***`, not the original token.
      - `test_redacts_args`: logger emits `"token=%s", "hf_abc..."` → args are redacted.
      - `test_passes_non_string_args`: logger emits `"value=%d", 42` → no exception, record unchanged.
      - `test_short_hf_string_not_redacted`: literal string `"hf_short"` (length < 30 after `hf_`) is NOT redacted — only real tokens match the regex.
  </behavior>
  <action>
    Step 1: Implement `backend/services/token_resolver.py` per the `<interfaces>` block:
      - `resolve()`: iterate sources in (app, env, hf-cli) order. App = `settings_store.get_hf_token()`. Env = `os.environ.get("HF_TOKEN")` (also accept `HUGGING_FACE_HUB_TOKEN` per HF docs). HF-CLI = `huggingface_hub.get_token()`. For each non-empty candidate, call `huggingface_hub.whoami(token=tok)` to validate. On `huggingface_hub.errors.HfHubHTTPError` with status 401 (or any HTTP error), continue. First validated wins. Return `ResolvedToken(token, source, username=user['name'])`. Cache for 300 seconds keyed by (source, token-sha256) — invalidate via `invalidate()` for the Settings UI "Test now" button (Open Question #4 resolution).
      - `on_401(active_source)`: call `resolve(skip=frozenset({active_source}))`.
      - `state()`: return all 3 sources (regardless of validity) so the Settings UI can display them. Include masked token (`hf_…` + last 3 chars), whoami_ok, whoami_user.
      - `save_app_token(token)`: `settings_store.set_hf_token(token)` then `huggingface_hub.login(token=token, add_to_git_credential=False, new_session=False)`. Invalidate the cache. Pitfall #2: ALWAYS pass `add_to_git_credential=False`.
      - `clear_app_token(also_clear_hf_cli=False)`: `settings_store.clear_hf_token()`; if flag set, `huggingface_hub.logout()`. Invalidate cache.

    Step 2: Implement `backend/core/logging_filter.py`:
      - `HFTokenRedactor(logging.Filter)` with `_HF_TOKEN_RE = re.compile(r"hf_[A-Za-z0-9]{30,}")`.
      - `filter(record)` rewrites `record.msg` if str, `record.args` element-wise if tuple of str. Always returns True.
      - `install_redaction_filter(root_logger=None)`: attaches the filter to `root_logger or logging.getLogger()` AND to every existing handler (in case handlers were configured before the filter installs). Idempotent — does not double-install.

    Step 3: Wire the filter into app startup. Edit `backend/main.py` (or whatever the FastAPI app factory file is — confirm via `grep -l "FastAPI(" backend/`) so that BEFORE any handler is added or any other module imports `logging.getLogger(...)`, we call `install_redaction_filter()`. Specifically: import as the FIRST line of the startup block. Per AUTH-05 + Pitfall threat T-01-02.

    Step 4: Patch all 5 confirmed HF_TOKEN read sites. Replace each bare `os.environ.get("HF_TOKEN")` with `token_resolver.resolve()`:
      a. `backend/api/routers/dub_core.py:540` — REPLACE the `if not os.environ.get("HF_TOKEN")` branch with `resolved = token_resolver.resolve(); if not resolved:`. Update the user-facing `reason` string to mention all three sources (per RESEARCH.md "Reading the resolver result" code example). When `resolved` is truthy but `diar_pipe` is still None, the message changes to reference `resolved.source` and `resolved.username` to aid diagnosis.
      b. `backend/api/routers/system.py:32` — same pattern. Adjust the surrounding API contract to return source attribution if the caller asked for it (read existing call site to determine the response shape; if it just returns a bool, that's fine — `bool(token_resolver.resolve())` is the replacement).
      c. `backend/services/sonitranslate.py:143` and `:217` — both pass `hf_token` into `sonitranslate` library calls. Replace with `resolved = token_resolver.resolve(); hf_token = resolved.token if resolved else ""`. Preserve the empty-string fallback used by `sonitranslate` library when no token.
      d. `backend/services/model_manager.py:480` — same pattern as (c).

    Step 5: Re-run the grep to confirm zero remaining `os.environ.*HF_TOKEN` or `os.getenv.*HF_TOKEN` call sites in `backend/` (excluding the token_resolver itself which IS the canonical reader, and test fixtures which simulate env). Implementation per Pitfall #1.

    Step 6: Write the test files described in `<behavior>`. Mock `huggingface_hub.whoami` via `monkeypatch` or `respx`. Mock `settings_store.get_hf_token` for the priority tests. For the cascade test, monkeypatch `whoami` to return `{"name": "alice"}` for one token and raise `HfHubHTTPError` (with 401 status) for the other.
  </action>
  <verify>
    <automated>uv run pytest tests/backend/services/test_token_resolver.py tests/backend/core/test_logging_filter.py -x -v && grep -RnE "os\.(environ|getenv).*HF_TOKEN" backend/ --include='*.py' | grep -v token_resolver.py | grep -v '^[[:space:]]*#' | wc -l | awk '{ if ($1 != 0) { print "FAIL: stray HF_TOKEN reads remain"; exit 1 } else { print "OK: zero stray reads" } }'</automated>
  </verify>
  <done>
    All 11 unit tests pass, grep confirms zero stray `os.environ.get("HF_TOKEN")` reads outside the resolver, logging filter is installed at app startup, and `caplog`-based test asserts no `hf_*` token bytes reach handlers.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Subprocess env injection + Settings save/clear API endpoints</name>
  <files>backend/api/routers/settings.py, backend/services/gpu_sandbox.py, backend/services/tts_backend.py, tests/backend/test_engine_spawn_token.py</files>
  <behavior>
    - When any backend subprocess launcher spawns a child with `Popen` or `mp.Process`, the child's environment contains `HF_TOKEN=<resolved.token>` when `token_resolver.resolve()` is non-None; the child's env does NOT contain `HF_TOKEN` (i.e. it inherits the parent's, which may be empty) when `resolve()` is None — never spawn with a literal empty HF_TOKEN that overrides a child-set default.
    - `POST /api/settings/hf-token` with body `{"token": "hf_..."}` from a loopback origin (e.g. `http://localhost:3900`) succeeds and the token round-trips via `settings_store.get_hf_token()`.
    - `POST /api/settings/hf-token` from a non-loopback origin returns 403 (per loopback origin check from quick task 260518-ivy, threat T-01-03).
    - `DELETE /api/settings/hf-token` clears the App-source token; resolver subsequently falls back to Env or HF-CLI.
    - `GET /api/settings/hf-token/state` returns the 3-source state JSON shaped for the frontend Settings panel (`{sources: [...], active: "app"|"env"|"hf-cli"|null}`).
  </behavior>
  <action>
    Step 1: Identify subprocess launch sites. Grep `backend/` for `subprocess.Popen(`, `subprocess.run(`, `multiprocessing.Process(`, and `mp.get_context(`. Confirmed sites likely include `backend/services/gpu_sandbox.py` (per RESEARCH.md Pattern 3) and any TTS engine launchers in `backend/services/tts_backend.py`. For each launch site that spawns a Python child running engine code (NOT short-lived tool calls like `ffprobe`), inject the resolved token into the child env:
      ```
      import os
      from backend.services import token_resolver
      _env = os.environ.copy()
      _resolved = token_resolver.resolve()
      if _resolved:
          _env["HF_TOKEN"] = _resolved.token
      # then pass env=_env to Popen/Process
      ```
    Be conservative: only patch launchers that obviously need HF auth (engines that download models from HF Hub). Skip ffprobe/ffmpeg/uv invocations. Document the launchers patched in the task SUMMARY.

    Step 2: Create `backend/api/routers/settings.py` (new router file). Three endpoints:
      - `POST /api/settings/hf-token` body `{token: str}` → `token_resolver.save_app_token(token)` → return updated state.
      - `DELETE /api/settings/hf-token` query `also_clear_hf_cli=bool` → `token_resolver.clear_app_token(also_clear_hf_cli)` → return updated state.
      - `GET /api/settings/hf-token/state` → return `token_resolver.state()` serialized.
      Apply the loopback origin check helper from `backend/api/routers/system.py` (the one shipped in commit `e1f08a6` for `/system/set-env`) as a FastAPI dependency on the POST and DELETE handlers (threat T-01-03). Mount the router in `backend/main.py` (or the app factory).

    Step 3: Write `tests/backend/test_engine_spawn_token.py`:
      - Use `TestClient` from FastAPI and a monkeypatched `token_resolver.resolve()` returning a fake token.
      - Test 1: POST `/api/settings/hf-token` from loopback (default Host) → 200.
      - Test 2: POST same endpoint with `Host: 10.0.0.5:3900` header → 403.
      - Test 3: After POST, the env block injected into a mocked Popen call (monkeypatch `subprocess.Popen` to capture kwargs) contains `HF_TOKEN=<the token>`.
      - Test 4: `clear_app_token()` then state shows `app.set == False`.

    Per AUTH-04 (subprocess injection) and AUTH-03 backend half (Settings API endpoints; the React UI ships in Plan 01-02 along with the docs deeplink work).
  </action>
  <verify>
    <automated>uv run pytest tests/backend/test_engine_spawn_token.py -x -v</automated>
  </verify>
  <done>
    Settings POST/DELETE/GET endpoints are live and loopback-guarded, subprocess launchers inject `HF_TOKEN` from the resolver, all four tests pass, and the new router is mounted in the FastAPI app.
  </done>
</task>

</tasks>

<verification>
  After all three tasks:
  - `uv run pytest tests/backend/services/test_token_resolver.py tests/backend/services/test_settings_store.py tests/backend/core/test_logging_filter.py tests/backend/test_engine_spawn_token.py -x` → all green
  - `grep -RnE "os\.(environ|getenv).*HF_TOKEN" backend/ --include='*.py' | grep -v token_resolver.py | grep -v '^[[:space:]]*#'` → 0 lines
  - Manually start the backend (`uv run uvicorn backend.main:app`) and `curl http://localhost:3900/api/settings/hf-token/state` returns valid JSON with 3 source entries
  - Manually run `alembic current` then `alembic upgrade head` then `alembic current` — version advances to `0001_phase1_settings`
</verification>

<success_criteria>
1. The `dub_core.py:540` bug class is dead: any code that wants to know whether an HF token is available calls `token_resolver.resolve()`, not `os.environ.get("HF_TOKEN")`. Grep gate enforces this in CI.
2. A user who ONLY ran `huggingface-cli login` (no env var, no app-store token) gets a working diarization pipeline on next OmniVoice launch.
3. A user who saves a token in the Settings API endpoint sees it survive `kill -9` of the backend + restart.
4. Encrypted SQLite column: `sqlite3 omnivoice.db "SELECT value FROM settings WHERE key='hf_token'"` returns ciphertext, never plaintext `hf_*`.
5. `caplog` asserts redaction works: no `hf_[A-Za-z0-9]{30,}` substring appears in any captured log record across the full test suite.
6. Alembic migration applies cleanly to a v0.2.7 fixture DB and reverses on downgrade.
</success_criteria>

<output>
Create `.planning/phases/01-install-token-persistence-docs-scaffolding-error-ux/01-01-SUMMARY.md` when done. Include:
- The exact list of subprocess launch sites patched in Task 3 (so Plan 02 + the Phase 2 SubprocessBackend work doesn't re-patch them)
- Confirmation of `cryptography` version actually present
- Any alembic head chain notes (if `0001_phase1_settings` had to chain from an existing head, name it)
- Note: `backend/core/links.py` is NOT created by this plan — Plan 01-02 owns it (per checker B-6 resolution).
</output>
