---
phase: 06-release-verification-retro
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - frontend/package.json
  - frontend/src-tauri/Cargo.toml
  - frontend/src-tauri/tauri.conf.json
  - frontend/src-tauri/Cargo.lock
  - README.md
  - CHANGELOG.md
  - tests/test_version_consistency.py
  - tests/test_changelog.py
  - .planning/release/v0.3.0-runbook.md
  - .planning/release/verification-matrix.md
  - .planning/retros/v0.3.0-retro.md
  - .planning/decisions/pr-73-strategy.md
autonomous: true
requirements:
  - REL-01
  - REL-03
  - REL-06
must_haves:
  truths:
    - "Version string `0.3.0` is identical across all 4 sources of truth + README badge URLs"
    - "CHANGELOG.md has a `## [0.3.0]` section with structured Added/Fixed/Documented Workaround/Deferred/Verification grid"
    - "`pytest tests/test_version_consistency.py tests/test_changelog.py` passes"
    - "Runbook + verification matrix + retro stub + PR-73 decision doc exist under `.planning/release/` and `.planning/retros/`"
    - "GitHub Milestone `v0.3.0` exists and contains all 11 originally-open + addition issues"
  artifacts:
    - path: pyproject.toml
      provides: "version=0.3.0"
      contains: 'version = "0.3.0"'
    - path: frontend/package.json
      provides: "version=0.3.0"
      contains: '"version": "0.3.0"'
    - path: frontend/src-tauri/Cargo.toml
      provides: "version=0.3.0"
      contains: 'version = "0.3.0"'
    - path: frontend/src-tauri/tauri.conf.json
      provides: "version=0.3.0"
      contains: '"version": "0.3.0"'
    - path: CHANGELOG.md
      provides: "v0.3.0 dated section + verification grid"
      contains: "## [0.3.0]"
    - path: tests/test_version_consistency.py
      provides: "4-source version grep assertion"
      min_lines: 20
    - path: tests/test_changelog.py
      provides: "## [X.Y.Z] dated header assertion"
      min_lines: 15
    - path: .planning/release/v0.3.0-runbook.md
      provides: "4-OS clean-VM exercise checklist"
      min_lines: 60
    - path: .planning/release/verification-matrix.md
      provides: "per-issue closure end-state grid"
      min_lines: 30
    - path: .planning/retros/v0.3.0-retro.md
      provides: "retro stub with 3-metric structure"
      min_lines: 20
    - path: .planning/decisions/pr-73-strategy.md
      provides: "reimplement-vs-rebase decision record"
      min_lines: 15
  key_links:
    - from: README.md
      to: "github.com/debpalash/OmniVoice-Studio/releases/download/v0.3.0/"
      via: "badge href URLs"
      pattern: "v0\\.3\\.0/OmniVoice\\.Studio_0\\.3\\.0"
    - from: CHANGELOG.md
      to: ".github/workflows/release.yml awk extractor"
      via: "## [0.3.0] section header matches tag"
      pattern: "^## \\[0\\.3\\.0\\] — 2[0-9]{3}-[0-9]{2}-[0-9]{2}$"
---

<objective>
Prepare the v0.3.0 release artifacts: bump the version in all 4 sources of truth + README badges, author the CHANGELOG `## [0.3.0]` section with the verification grid, scaffold the 4-OS clean-VM runbook + verification matrix + retro doc + PR-73 strategy decision, add version + CHANGELOG consistency tests, and set up the GitHub Milestone for issue tracking. No tag is pushed in this plan — that's Wave 2.

Purpose: Eliminate the documented past failure mode (`## [0.2.7] — Unreleased` / `## [0.2.6] — Unreleased` markers in CHANGELOG.md mean prior releases tagged without finalized notes). This plan makes that structurally impossible for v0.3.0 by gating tag-push behind two new pytest assertions, and prepares every doc-only artifact Wave 2 will reference at tag-cut time.

Output: PR titled `chore(release): v0.3.0 changelog + version bump` (single PR, deliberate edit, lands on `main` before tagging). Plus a milestone with all closure-target issues attached, ready for `gh issue list --milestone v0.3.0` retro queries.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-release-verification-retro/06-RESEARCH.md
@CLAUDE.md
@CHANGELOG.md
@pyproject.toml
@frontend/package.json
@frontend/src-tauri/Cargo.toml
@frontend/src-tauri/tauri.conf.json
@README.md
@.github/workflows/release.yml

<interfaces>
Version sources of truth (verified by grep at planning time):
- `pyproject.toml:7`                      => `version = "0.2.7"`     => bump to `version = "0.3.0"`
- `frontend/package.json:4`               => `"version": "0.2.7",`   => bump to `"version": "0.3.0",`
- `frontend/src-tauri/Cargo.toml:3`       => `version = "0.2.7"`     => bump to `version = "0.3.0"`
- `frontend/src-tauri/tauri.conf.json:4`  => `"version": "0.2.7",`   => bump to `"version": "0.3.0",`

README badge URLs (verified at `README.md:25-28`):
- Each line of form `releases/download/v0.2.7/OmniVoice.Studio_0.2.7_<arch>.<ext>`
- Replace `v0.2.7/OmniVoice.Studio_0.2.7` with `v0.3.0/OmniVoice.Studio_0.3.0` on lines 25-28 only.

CHANGELOG.md state at planning time:
- Top entry is `## [0.2.7] — Unreleased` (the failure-mode marker — keep as historical record, do NOT retroactively date it; release.yml's awk extractor scans for the section matching the pushed tag, so old `Unreleased` markers do not affect v0.3.0).
- Insert NEW `## [0.3.0] — UNRELEASED` section ABOVE the `## [0.2.7]` entry. Wave 2 replaces `UNRELEASED` with real `YYYY-MM-DD` date at tag-cut time.
- `tests/test_changelog.py` only enforces date format when `RELEASE_GATE=1` env var is set; default-skip so CI on `main` does not fail on the sentinel.

release.yml CHANGELOG extractor (`release.yml:322-337`, do NOT modify here):
`awk -v tag="$TAG" '/^## \[/ { if (in_section) exit; if ($0 ~ "\\[" tag "\\]") { in_section = 1; next } } in_section { print }' CHANGELOG.md`
Matches the line `## [0.3.0]` exactly. Section body is everything until the next `## [` header.

Originally-open issues for milestone (from 06-RESEARCH `gh issue list --state open --limit 20`):
  35, 42, 48, 54, 55, 56, 57, 58, 60, 65, 72, 76, 80
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Version-consistency + CHANGELOG date assertions (tests first)</name>
  <files>tests/test_version_consistency.py, tests/test_changelog.py</files>
  <behavior>
    - test_version_consistency.py::test_all_version_sources_match:
      * Reads `pyproject.toml`, `frontend/package.json`, `frontend/src-tauri/Cargo.toml`, `frontend/src-tauri/tauri.conf.json`.
      * Extracts the version string from each using stdlib only (`tomllib` for TOML, `json` for JSON — no third-party deps).
      * Asserts all 4 values are identical and match SEMVER `\d+\.\d+\.\d+(-[A-Za-z0-9.-]+)?`.
      * Asserts README.md badge URLs on the four download-badge lines reference the SAME version (regex `v(\d+\.\d+\.\d+)/OmniVoice\.Studio_\1_`).
    - test_changelog.py::test_release_section_has_date:
      * Reads `CHANGELOG.md` top-of-file.
      * Finds the FIRST `## [X.Y.Z]` header.
      * Under env `RELEASE_GATE=1`: asserts header matches `^## \[\d+\.\d+\.\d+\] — \d{4}-\d{2}-\d{2}$` (real date, not `Unreleased` / `UNRELEASED`).
      * Without `RELEASE_GATE=1`: skips with `pytest.skip("release-gate test; set RELEASE_GATE=1 to enforce")` — keeps `main` green while sentinel is in place.
  </behavior>
  <action>Per REL-03 and Pitfalls 1 and 2 in 06-RESEARCH: write both pytest files BEFORE editing any version source. Place in `tests/` (not `backend/tests/` — these are repo-wide release gates, not backend-domain). Use `tomllib` (Python 3.11+) for pyproject + Cargo.toml; use `json` for the two JSON files; use `re` for README badge URL extraction. Make both tests parameter-free (no fixtures, no conftest changes). For `test_changelog.py`, gate the strict assertion behind `os.environ.get("RELEASE_GATE") == "1"` and explicitly call `pytest.skip()` otherwise — Wave 2 will set `RELEASE_GATE=1` in its pre-tag verification step. Run the tests after writing them — `test_all_version_sources_match` will PASS (all four sources currently read 0.2.7 consistently); `test_release_section_has_date` will SKIP without the env var. Confirm RED behaviour by running once with `RELEASE_GATE=1` BEFORE Task 3 — it must FAIL because no `## [0.3.0]` section exists yet. Document that one-shot RED check in the test's docstring.</action>
  <verify>
    <automated>uv run pytest tests/test_version_consistency.py tests/test_changelog.py -v</automated>
  </verify>
  <done>Both test files exist. `test_all_version_sources_match` passes. `test_release_section_has_date` skips without `RELEASE_GATE=1`. With `RELEASE_GATE=1`, the changelog test FAILS before Task 3 runs (no v0.3.0 section yet) — documented in a one-shot manual run.</done>
</task>

<task type="auto">
  <name>Task 2: Bump version across 4 sources + README badges + Cargo.lock</name>
  <files>pyproject.toml, frontend/package.json, frontend/src-tauri/Cargo.toml, frontend/src-tauri/tauri.conf.json, frontend/src-tauri/Cargo.lock, README.md</files>
  <action>Edit version string from `0.2.7` to `0.3.0` in all four sources of truth per the `<interfaces>` extraction. Use the Edit tool, one file at a time, not sed — Edit gives line-anchored precision and avoids accidental matches in dependency strings (e.g., `serde = "0.2.7"` if it existed). For each file: pyproject.toml line 7, frontend/package.json line 4, frontend/src-tauri/Cargo.toml line 3, frontend/src-tauri/tauri.conf.json line 4 — replace `0.2.7` with `0.3.0` ONLY on those specific lines. For README.md lines 25-28: replace `v0.2.7/OmniVoice.Studio_0.2.7_` with `v0.3.0/OmniVoice.Studio_0.3.0_` on each of the four badge `<a href>` lines. Then regenerate `frontend/src-tauri/Cargo.lock` by running `cd frontend/src-tauri && cargo update -p omnivoice-studio-tauri --offline 2>&1 || cargo update -p omnivoice-studio-tauri` (offline first since this is a self-version bump that does NOT pull new external deps; fall back to online if the offline path can't update self-references). Re-run `pytest tests/test_version_consistency.py -v` immediately after — must pass with all four sources reading `0.3.0`. Per scope-decision: the build-time templater is out of scope for this release; the 4-source edit pattern is the intentional approach for v0.3.0, and a single-source templater is captured as a v0.4 deferred improvement in `.planning/decisions/pr-73-strategy.md`'s "lessons learned" section (added in Task 4).</action>
  <verify>
    <automated>uv run pytest tests/test_version_consistency.py -v && grep -c 'v0\.3\.0/OmniVoice\.Studio_0\.3\.0' README.md</automated>
  </verify>
  <done>All four sources show `0.3.0`. README has exactly 4 badge URLs pointing at `v0.3.0/OmniVoice.Studio_0.3.0_*`. `pytest tests/test_version_consistency.py` passes. `Cargo.lock` updated for the self-package version bump. No other `0.2.7` references introduced or removed except in CHANGELOG history.</done>
</task>

<task type="auto">
  <name>Task 3: Author CHANGELOG ## [0.3.0] section with verification grid</name>
  <files>CHANGELOG.md</files>
  <action>Insert a new `## [0.3.0] — UNRELEASED` section ABOVE the existing `## [0.2.7] — Unreleased` entry. Use the section structure from 06-RESEARCH "Pattern 4: Release notes from CHANGELOG section": header note about empty-the-inbox milestone (closes all 11 originally-open issues), then sections in this order: `### Added`, `### Fixed`, `### Documented Workaround (closed via docs + error UI)`, `### Deferred to v0.4 (tracking issues filed)`, `### Closed without code change`, `### Verification grid`. The Verification grid is a markdown table with columns `Issue | Closed by | Verified by | End state`. For each of the 13 originally-open issues (35, 42, 48, 54, 55, 56, 57, 58, 60, 65, 72, 76, 80) write one row; use placeholders `PR #TBD` and `tracking issue #TBD` where the closing PR/issue number is not yet known — Wave 2's retro task fills these from `gh issue list --milestone v0.3.0 --state closed`. For "Added" section, write Supertonic-3 (Phase 3 TTS-01..06), Opt-in bug reporting (Phase 5 REPORT-01..12), HF token persistence (Phase 1 AUTH-01..06), Per-OS install docs, Error UX deeplinks, Engine Compatibility Matrix UI. Add `*[GGUF/Singing entries — pending SPIKE-01/SPIKE-02 outcomes; Wave 2 finalizes]*` placeholder under Added per 06-RESEARCH Open Question 4. Use uppercase `UNRELEASED` as the sentinel so it's distinguishable from the historical lowercase `Unreleased` markers below it; Wave 2 replaces `UNRELEASED` with the real `YYYY-MM-DD` date. Per REL-03: every row in the Verification grid points to a commit/PR/docs path — leave `PR #TBD` placeholders where unknown, but DO populate the `Verified by` and `End state` columns now since those are stable (e.g., `manual clean-VM exercise`, `Fixed`, `Documented workaround`). Leave the SHA-256 checksums footer empty — release.yml auto-appends.</action>
  <verify>
    <automated>grep -c "^## \[0\.3\.0\] — UNRELEASED" CHANGELOG.md; awk '/^## \[0\.3\.0\]/{flag=1;next}/^## \[/{flag=0}flag' CHANGELOG.md | grep -cE "^### (Added|Fixed|Documented Workaround|Deferred|Closed without|Verification)"</automated>
  </verify>
  <done>CHANGELOG.md has exactly 1 `## [0.3.0] — UNRELEASED` line and 6 `###` subsections under it. Verification grid contains rows for all 13 originally-open issues. `RELEASE_GATE=1 pytest tests/test_changelog.py` still FAILS (expected — date is UNRELEASED until Wave 2).</done>
</task>

<task type="auto">
  <name>Task 4: Scaffold runbook + verification matrix + retro stub + PR-73 decision doc + create v0.3.0 milestone</name>
  <files>.planning/release/v0.3.0-runbook.md, .planning/release/verification-matrix.md, .planning/retros/v0.3.0-retro.md, .planning/decisions/pr-73-strategy.md</files>
  <action>Create the four planning artifacts. (1) `.planning/release/v0.3.0-runbook.md` — copy the "Pattern 3: Manual 4-OS clean-VM exercise checklist" from 06-RESEARCH verbatim into this file (~70 lines), expanded with: a pre-flight section (rc1 tagged, release.yml green on all 3 matrix legs, draft release exists with DMG/MSI/AppImage/deb attached, SHA256SUMS-*.txt published, all 4 VMs reset to clean snapshot), per-OS sections (UTM macOS Sequoia, Hyper-V Windows 11, Ubuntu 24.04, Fedora 44) each as a `- [ ]` checklist (download installer; verify SHA-256 against the published file; open the relevant docs/install/<os>.md side-by-side; follow every step verbatim, no shortcuts; hit the documented workaround that exists for that OS — `xattr -cr` for macOS, Triton toggle for Windows, ffprobe path for Ubuntu deb, WEBKIT_DISABLE_COMPOSITING_MODE=1 for Fedora; confirm in-app error-UI surfacing of the workaround link works per Phase 1 INST-03/INST-04; reach a working voice-clone output; HF-token-persists-across-restart check; sign-off line). Add a "Backward-compat data check" subsection per 06-RESEARCH Runtime State Inventory: install v0.2.7, populate sample voices/projects, upgrade-install v0.3.0, confirm omnivoice_data intact and tray + global hotkey persist. Add an "Outcomes" footer: all four sign-off => rc1 promotes to v0.3.0 after 48h soak; any one fails => cut rc2, fix, restart 48h. (2) `.planning/release/verification-matrix.md` — table with one row per originally-open issue (13 rows for 35,42,48,54,55,56,57,58,60,65,72,76,80), columns `Issue | Title | Closed by | Verified by | End state (Fixed | Documented workaround | Deferred-to-v0.4 | Closed-without-code-change) | Wave-2 verifier signoff`. Pre-fill the End state column from Phase 0-5 ROADMAP entries; leave Closed-by PR numbers and signoff cells empty for Wave 2 to fill. (3) `.planning/retros/v0.3.0-retro.md` — stub with three top-level sections: `## Metrics` (sub: Weighted closure count, Net inbox change, Discord support-volume delta on install/HF-token/dubbing — each with a "Source", "Method", "Result: TBD" line per Pitfall 6 in 06-RESEARCH; for Discord, note that admin access is ASSUMED and if unavailable the metric is qualitative-only); `## What Worked` (stub); `## What To Change` (stub); each section ~5-10 lines so Wave 2 retro authoring has a template. (4) `.planning/decisions/pr-73-strategy.md` — 1-page rationale: PR #73 is 93 commits behind main with conflicts in `lib.rs` (pill-mode widget creation merged 2026-05-17 per b479f9b) and `tauri.conf.json` (widget window config); auto-rebase risks silently dropping widget creation per Pitfall 3; recommendation is reimplement-over-rebase in Wave 2 (plan 06-03) with same 4-file scope (~180 lines) written against post-PR-84 main; "lessons learned" sub-section noting build-time version templater as v0.4 deferred improvement. After creating all four files, set up the GitHub Milestone: `gh api -X POST /repos/debpalash/OmniVoice-Studio/milestones -f title=v0.3.0 -f state=open --silent 2>/dev/null || true`, then for each issue in `35 42 48 54 55 56 57 58 60 65 72 76 80`: `gh issue edit <n> --milestone v0.3.0` (idempotent — re-running does no harm). Capture the milestone URL and write it as a header line in `.planning/retros/v0.3.0-retro.md` so Wave 2 can dereference.</action>
  <verify>
    <automated>test -f .planning/release/v0.3.0-runbook.md && test -f .planning/release/verification-matrix.md && test -f .planning/retros/v0.3.0-retro.md && test -f .planning/decisions/pr-73-strategy.md && wc -l .planning/release/v0.3.0-runbook.md .planning/release/verification-matrix.md .planning/retros/v0.3.0-retro.md .planning/decisions/pr-73-strategy.md && gh api /repos/debpalash/OmniVoice-Studio/milestones --jq '.[] | select(.title=="v0.3.0") | .number'</automated>
  </verify>
  <done>All four planning files exist with minimum line counts per must_haves. Each of the 13 issues is assigned to the v0.3.0 milestone (verified via `gh issue list --milestone v0.3.0 --state all` returning 13+ entries). Retro stub references the milestone URL.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local repo to GitHub API | `gh api` calls + `gh issue edit` cross into the GitHub plane; the `gh` CLI uses the user's PAT/SSO from `~/.config/gh` |
| CHANGELOG.md to release.yml awk | maintainer-curated text is consumed by CI parsing as release body |
| version sources to release.yml | the four version files drive installer filenames + Updater manifest signature |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.01-01 | Tampering | CHANGELOG.md awk parsing in release.yml | mitigate | `## [0.3.0]` header regex anchored by tests/test_changelog.py with `RELEASE_GATE=1`; Task 1 gates this before Wave 2 tag-push |
| T-06.01-02 | Tampering | Version sources of truth (4 files) | mitigate | tests/test_version_consistency.py asserts all 4 sources + README badges agree; Task 1 makes drift structurally visible |
| T-06.01-03 | Information Disclosure | Milestone setup via `gh api` | accept | `gh` CLI authenticates via maintainer's own session; no new secret introduced; idempotent so re-run does no harm |
| T-06.01-04 | Repudiation | Verification matrix as audit trail | mitigate | per-issue closure end-state is written to `verification-matrix.md` with explicit "Wave-2 verifier signoff" column; Wave 2 cannot promote rc1 without filling that column |
| T-06.01-SC | Tampering | Supply chain (package installs) | n/a | Plan 06-01 adds zero packages — only stdlib tomllib + json. No install tasks, no legitimacy gate required. |
</threat_model>

<verification>
- `uv run pytest tests/test_version_consistency.py tests/test_changelog.py -v` => 1 pass + 1 skip (default mode)
- `RELEASE_GATE=1 uv run pytest tests/test_changelog.py -v` => FAIL until Wave 2 dates the section (intentional; documents the gate)
- `grep -E "^## \[0\.3\.0\] — UNRELEASED$" CHANGELOG.md` => exactly 1 match
- `grep -c "0.3.0" pyproject.toml frontend/package.json frontend/src-tauri/Cargo.toml frontend/src-tauri/tauri.conf.json` => each file has at least 1 hit; verify by reading
- `gh api /repos/debpalash/OmniVoice-Studio/milestones --jq '.[] | select(.title=="v0.3.0") | .title'` => returns `v0.3.0`
- `gh issue list --milestone v0.3.0 --state all --json number --jq 'length'` => >=13
</verification>

<success_criteria>
- Version `0.3.0` appears identically in all four sources + README badge URLs
- CHANGELOG.md has the `## [0.3.0] — UNRELEASED` section with the verification grid containing all 13 originally-open issue rows
- Both new pytest files exist and behave per their docstrings
- `.planning/release/v0.3.0-runbook.md`, `.planning/release/verification-matrix.md`, `.planning/retros/v0.3.0-retro.md`, `.planning/decisions/pr-73-strategy.md` all exist with minimum line counts
- `v0.3.0` milestone exists in GitHub with all 13 originally-open issues attached
- All changes committed; Wave 2 can `gh issue list --milestone v0.3.0 --state closed` to populate the verification grid at retro authoring time
</success_criteria>

<output>
Create `.planning/phases/06-release-verification-retro/06-01-SUMMARY.md` when done.
</output>
