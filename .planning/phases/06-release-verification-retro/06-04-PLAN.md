---
phase: 06-release-verification-retro
plan: 04
type: execute
wave: 2
depends_on:
  - 06-01
  - 06-02
  - 06-03
files_modified:
  - CHANGELOG.md
  - .planning/release/v0.3.0-runbook.md
  - .planning/release/verification-matrix.md
  - .planning/retros/v0.3.0-retro.md
autonomous: false
requirements:
  - REL-01
  - REL-02
  - REL-03
  - REL-04
  - REL-05
  - REL-06
must_haves:
  truths:
    - "Tag `v0.3.0-rc1` is pushed and `release.yml` produces signed installers + SHA-256 checksums for all 3 matrix legs"
    - "All 4 clean-VM exercise checklists in v0.3.0-runbook.md are signed off (UTM macOS Sequoia, Hyper-V Windows 11, Ubuntu 24.04, Fedora 44)"
    - "48-hour soak window completes with no regression report on Discord, GH Issues, or auto-bug-report inbox"
    - "Tag `v0.3.0` is pushed; draft release is promoted via `gh release edit v0.3.0 --draft=false`; SHA-256 checksums are attached"
    - "CHANGELOG.md `## [0.3.0]` section is dated (no UNRELEASED sentinel); RELEASE_GATE=1 pytest passes; verification grid is fully populated"
    - "Retrospective is published with the 3 metrics (weighted closure count, net inbox change, Discord support-volume delta)"
    - "3 deferral tracking issues exist: macOS code signing + notarization, Tauri/WebKit Fedora upstream, per-engine subprocess hardening beyond IndexTTS"
    - "All 11 originally-open issues are in a confirmed end state (Closed via fix, Closed via documented workaround + UI, or moved to v0.4 milestone)"
  artifacts:
    - path: CHANGELOG.md
      provides: "## [0.3.0] section with real date + populated verification grid"
      contains: "## [0.3.0] — 2"
    - path: .planning/release/v0.3.0-runbook.md
      provides: "completed runbook with 4-OS sign-offs"
      min_lines: 80
    - path: .planning/release/verification-matrix.md
      provides: "filled verification matrix with PR numbers + signoffs"
      min_lines: 30
    - path: .planning/retros/v0.3.0-retro.md
      provides: "published retro with 3 metrics + lessons learned"
      min_lines: 60
  key_links:
    - from: "git tag v0.3.0-rc1 + v0.3.0"
      to: "GitHub Release with attached installers + SHA256SUMS-*.txt"
      via: "release.yml tag-push trigger"
      pattern: "gh release view v0\\.3\\.0"
    - from: "GH milestone v0.3.0"
      to: ".planning/retros/v0.3.0-retro.md weighted closure count"
      via: "gh issue list --milestone v0.3.0 --state closed"
      pattern: "weighted.closure"
    - from: "Tracking issues filed via gh issue create"
      to: ".planning/retros/v0.3.0-retro.md deferred section"
      via: "v0.4 milestone + deferred-from-v0.3 label"
      pattern: "deferred-from-v0\\.3"
---

<objective>
Execute the release: cut `v0.3.0-rc1`, run the 4-OS clean-VM exercise, soak for 48 hours, promote to `v0.3.0`, publish the retrospective, file the 3 deferral tracking issues, and confirm all 11 originally-open issues are in their final end state.

Purpose: This is the closure-and-verify wave. Wave 1 prepared every artifact (CHANGELOG section, runbook, milestone, version-consistency tests, CI guards). 06-03 reimplemented PR #73. Now we cut the actual tag and shepherd it through the soak.

Output: 
- Published GitHub Release at `v0.3.0` with DMG / MSI / AppImage / .deb + SHA256SUMS-*.txt + signed updater manifest + backend tar.gz per platform (the latter from 06-03).
- CHANGELOG.md `## [0.3.0]` section dated `YYYY-MM-DD` with the verification grid fully populated.
- `.planning/retros/v0.3.0-retro.md` published with the 3 metrics filled in.
- 3 tracking issues filed on the `v0.4` milestone.
- All 11 originally-open issues in a confirmed end state.

The plan is non-autonomous (`autonomous: false`) — it contains 3 `checkpoint:human-verify` tasks and 1 `checkpoint:decision` task. The wall-clock soak is a literal wait, not automation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-release-verification-retro/06-RESEARCH.md
@.planning/release/v0.3.0-runbook.md
@.planning/release/verification-matrix.md
@.planning/retros/v0.3.0-retro.md
@CLAUDE.md
@CHANGELOG.md
@.github/workflows/release.yml

<interfaces>
Pre-tag gates that MUST pass before pushing v0.3.0-rc1:
- All Wave 1 + 06-03 PRs merged to main (06-01, 06-02, 06-03)
- `uv run pytest tests/test_version_consistency.py -v` => pass
- `gh run list --workflow=ci.yml --limit 3 --branch main` => latest run is `completed/success`
- `gh release view v0.3.0-rc1 2>/dev/null` => NOT FOUND (we have not tagged yet)
- `git rev-parse --abbrev-ref HEAD` => `main`

rc1 cut commands:
```
git tag -a v0.3.0-rc1 -m "v0.3.0-rc1: Stabilization milestone release candidate 1"
git push origin v0.3.0-rc1
gh run watch
```
After release.yml succeeds across all 3 matrix legs, manually mark the GH Release as pre-release (since release.yml defaults `prerelease: false`):
```
gh release edit v0.3.0-rc1 --prerelease
```

48-hour soak monitoring cadence (per Pitfall 5 in 06-RESEARCH):
- Calendar block 2x/day for Discord + GH Issues + auto-bug-report inbox check
- ANY regression report restarts the soak window with rc2 — do NOT promote rc1 if a regression lands
- Soak window starts at the moment release.yml completes for v0.3.0-rc1, NOT at the moment the tag is pushed

Promote to v0.3.0 (after clean soak):
```
# CRITICAL: date the CHANGELOG section FIRST
# Edit CHANGELOG.md: replace `## [0.3.0] — UNRELEASED` with `## [0.3.0] — YYYY-MM-DD`
# Commit the date change to main BEFORE tagging
git add CHANGELOG.md
git commit -m "chore(release): date v0.3.0 changelog section"
git push origin main
# Verify RELEASE_GATE
RELEASE_GATE=1 uv run pytest tests/test_changelog.py -v   # MUST pass now
# Tag
git tag -a v0.3.0 -m "v0.3.0: Stabilization milestone"
git push origin v0.3.0
gh run watch
# After release.yml completes, promote to non-draft (per Anti-pattern in 06-RESEARCH)
gh release edit v0.3.0 --draft=false
```

Retro data collection commands:
```
# Weighted closure count
gh issue list --milestone v0.3.0 --state closed --limit 100 \
  --json number,title,closedAt,closedByPullRequestsReferences,labels \
  > .planning/retros/v0.3.0-closure-data.json

# Net inbox change (open vs closed during milestone window)
MILESTONE_START=2026-05-16
gh issue list --search "is:issue created:>=$MILESTONE_START" \
  --state all --limit 200 \
  --json number,title,createdAt,state \
  > .planning/retros/v0.3.0-net-inbox.json
```

Deferral issue templates (per 06-RESEARCH "Deferral tracking issue template"):
1. v0.4: macOS code signing + notarization (real Apple Developer cert)
2. v0.4: Tauri/WebKit Fedora 44 white-screen upstream fix
3. v0.4: Per-engine subprocess hardening beyond IndexTTS

Each issue: label `v0.4,deferred-from-v0.3`; milestone `v0.4`; body documents context, acceptance criteria, out-of-scope. Create the `v0.4` milestone first if it does not exist:
```
gh api -X POST /repos/debpalash/OmniVoice-Studio/milestones -f title=v0.4 -f state=open --silent 2>/dev/null || true
```

11 originally-open issues for end-state confirmation:
35, 42, 48, 54, 55, 56, 57, 58, 60, 65, 72, 76, 80 (13 total; the "11" in REL-06 is the original CLAUDE.md count, expanded to 13 after #76 / #80 / #72 were added per the inbox grow during planning — record the discrepancy in the retro).

Retrospective structure (per `.planning/retros/v0.3.0-retro.md` stub from 06-01):
- Header: milestone URL, release URL, dates
- ## Metrics: (a) Weighted closure count with source/method/result; (b) Net inbox change; (c) Discord support-volume delta on install/HF-token/dubbing
  - For (c), per Pitfall 6 in 06-RESEARCH and scope decision #5, the metric can be qualitative if Discord admin access is unavailable
- ## What Worked: 4-VM exercise caught X; the 2-pass cargo build for backend SHA worked; ...
- ## What To Change: build-time version templater as v0.4 work; ...
- ## Cross-AI / Council notes: synthesize from any review feedback present in .planning/

Discord support-volume baseline establishment (decision point):
- Try `Discord admin history export` first. If not available: document as qualitative-only.
- This is a `checkpoint:decision` task — the user needs to authorize the time spent on Discord scraping vs. accepting qualitative.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-tag verification + cut v0.3.0-rc1</name>
  <files>(no file modifications — only git tag and gh API calls)</files>
  <action>Run all pre-tag gates from `<interfaces>` in order: (1) `git status` (clean working tree on main); (2) `git fetch origin && git rev-list --left-right --count main...origin/main` (no divergence); (3) `uv run pytest tests/test_version_consistency.py -v` (must pass); (4) `gh run list --workflow=ci.yml --limit 1 --branch main --json conclusion --jq '.[0].conclusion'` (must equal `success`); (5) `gh run list --workflow=release.yml --limit 1 --branch main --json conclusion --jq '.[0].conclusion'` (must equal `success` from the most recent merged PR's dry-run via 06-02); (6) verify `gh release view v0.3.0-rc1 2>&amp;1 | grep -q "release not found"` (rc1 not yet cut). If any gate fails, STOP and surface the failure. If all pass, cut rc1: `git tag -a v0.3.0-rc1 -m "v0.3.0-rc1: Stabilization milestone release candidate 1"` then `git push origin v0.3.0-rc1`. Watch the run: `gh run watch` (blocks until the workflow completes). After release.yml succeeds across all 3 matrix legs (macOS-14, windows-2022, ubuntu-22.04 per release.yml:122-145), mark the rc1 GH Release as pre-release: `gh release edit v0.3.0-rc1 --prerelease`. Capture the release URL, draft status, and SHA256SUMS-*.txt attachment list — write them as a section header in `.planning/release/v0.3.0-runbook.md` under a new "rc1 cut record" subsection so the next task has a clean handoff.</action>
  <verify>
    <automated>gh release view v0.3.0-rc1 --json tagName,isDraft,isPrerelease,assets --jq '{tag:.tagName,draft:.isDraft,prerelease:.isPrerelease,assetCount:(.assets|length)}' &amp;&amp; gh run list --workflow=release.yml --branch main --limit 1 --json conclusion --jq '.[0].conclusion'</automated>
  </verify>
  <done>Tag `v0.3.0-rc1` exists. GH Release at `v0.3.0-rc1` is marked pre-release. All 3 matrix legs produced installers + SHA256SUMS-*.txt. release.yml workflow conclusion is `success`. Runbook has the "rc1 cut record" subsection with the release URL.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 4-OS clean-VM exercise (UTM macOS Sequoia + Hyper-V Win 11 + Ubuntu 24.04 + Fedora 44)</name>
  <what-built>v0.3.0-rc1 is cut and signed installers are attached to the draft pre-release. The maintainer now follows .planning/release/v0.3.0-runbook.md verbatim on four clean VMs to satisfy REL-01.</what-built>
  <how-to-verify>
    1. Open `.planning/release/v0.3.0-runbook.md` and follow each OS section IN ORDER. No shortcuts — the explicit "no shortcuts" gate is in REL-01.
    2. UTM macOS Sequoia 15.x: download DMG from rc1 draft release; verify SHA-256 against published `SHA256SUMS-macOS Apple Silicon.txt`; open docs/install/macos.md SIDE-BY-SIDE; follow every step verbatim; hit the `xattr -cr` workaround per #54; confirm Phase 1 INST-03 in-app error surface link works; reach a working voice-clone output; enter HF token via Settings -> API Keys; restart app; verify token persists; sign off in the runbook checkbox.
    3. Hyper-V Windows 11: download MSI; verify SHA-256; follow docs/install/windows.md verbatim; hit the Triton/torch.compile workaround per #65; reach working clone output; HF token via PowerShell flow; sign off.
    4. Ubuntu 24.04 (Hyper-V or UTM): download .deb + AppImage on TWO separate clean snapshots; verify SHA-256 for both; test .deb path (`sudo dpkg -i ...`) per #76 (ffprobe location); test AppImage path on second snapshot; reach working clone output on each; sign off.
    5. Fedora 44 (UTM): download AppImage; verify SHA-256; hit WEBKIT_DISABLE_COMPOSITING_MODE=1 workaround per #56; confirm the launcher's conditional kicks in per Phase 1 INST-04; reach working clone output; sign off.
    6. Backward-compat data check (any one of the four OSes): install v0.2.7 first, populate sample voices/projects, upgrade-install v0.3.0-rc1, confirm `omnivoice_data/` intact, tray icon and global hotkey persist after upgrade. Sign off.
    7. If ANY checkbox fails: cut rc2 (a fresh tag `v0.3.0-rc2`), fix the issue, restart Tasks 1+2 against rc2. Do not silently mark a failure as OK — REL-01 is strict.
    8. Update `.planning/release/v0.3.0-runbook.md` with signoff lines (`- [x]` -> `- [x] signed off YYYY-MM-DD by <maintainer>`).
  </how-to-verify>
  <files>.planning/release/v0.3.0-runbook.md</files>
  <action>Human-verification checkpoint — pause execution and walk through the 4-OS runbook in `.planning/release/v0.3.0-runbook.md`. Sign off each OS section's checkboxes verbatim per how-to-verify steps 1-8 above. No automated action; the executor MUST stop and surface the resume-signal prompt to the user before proceeding.</action>
  <verify>
    <human-check>User confirms every checkbox in the runbook is checked + signed off with maintainer initials + date. Any failed OS triggers an rc2 cycle (re-run Tasks 1+2 against the rc2 tag).</human-check>
  </verify>
  <done>User responds "all 4 OSes signed off"; runbook file shows `- [x] signed off YYYY-MM-DD` lines for every per-OS checkbox plus the backward-compat data check.</done>
  <resume-signal>Type "all 4 OSes signed off" if every checkbox in the runbook is checked. Otherwise describe which OS failed and the failure mode — that triggers an rc2 cycle.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: 48-hour soak window with explicit 2x-daily monitoring</name>
  <what-built>All 4 OS sign-offs are recorded in the runbook. v0.3.0-rc1 is now in the 48-hour soak window per REL-02 + Pitfall 5 in 06-RESEARCH.</what-built>
  <how-to-verify>
    1. Calendar-block 2x/day check-ins for the next 48 hours starting NOW (Task 2 completion timestamp). Cadence: morning + evening, both in working hours so weekend gaps do not swallow regression reports.
    2. At each check-in, scan all 3 surfaces: (a) Discord #help and #bugs channels — anything tagged or mentioned about v0.3.0-rc1 or the rc draft release; (b) `gh issue list --state open --created ">=$(date -d '24 hours ago' --iso-8601)"` (recent issues); (c) the opt-in auto-bug-report inbox (Phase 5 REPORT-* — if any reports landed via the prefilled URL pattern they show up as labeled `auto-report` in GH Issues).
    3. If ANY regression report lands during the window: STOP. Cut rc2: fix the issue on a PR to main; merge; re-tag `v0.3.0-rc2`; restart Tasks 1+2+3 against rc2.
    4. Do NOT promote rc1 to v0.3.0 if a regression report exists, even if the maintainer thinks it is unrelated. The soak gate is per-rc, not per-perceived-severity.
    5. At T+48h (wall clock from Task 2 completion), if all 4 check-ins are clean: sign off on the soak. Update `.planning/release/v0.3.0-runbook.md` with the soak start + soak end timestamps and the per-check-in observations ("clean", "1 question about install path — not a regression", etc).
  </how-to-verify>
  <files>.planning/release/v0.3.0-runbook.md</files>
  <action>Human-monitoring checkpoint — start the 48-hour soak clock at this task's completion time. Calendar-block 2x/day check-ins per how-to-verify above. No automated action; the executor MUST stop, surface the resume-signal prompt, and wait for the user's signal at T+48h.</action>
  <verify>
    <human-check>User reports 48-hour wall-clock window completed with zero regression reports across Discord + GH Issues + auto-bug-report inbox. Runbook records soak start + end timestamps plus per-check-in observations.</human-check>
  </verify>
  <done>User responds "soak clean — promote rc1" — execution can resume to Task 4. Any regression report triggers cut-rc2 and restart of Tasks 1+2+3 against the new rc tag.</done>
  <resume-signal>Type "soak clean — promote rc1" if the 48-hour window passed with no regression. Otherwise type "soak hit a regression: <description>" to trigger rc2.</resume-signal>
</task>

<task type="auto">
  <name>Task 4: Date the CHANGELOG section, promote to v0.3.0 tag, publish GH Release</name>
  <files>CHANGELOG.md</files>
  <action>Replace `## [0.3.0] — UNRELEASED` with `## [0.3.0] — YYYY-MM-DD` where YYYY-MM-DD is today's date (use `date --iso-8601` or the current date from environment). Use Edit (not sed) for line-anchored precision. Run `RELEASE_GATE=1 uv run pytest tests/test_changelog.py -v` — MUST pass now (the gate from Wave 1 Task 1 finally goes green). Commit: `git add CHANGELOG.md` then `git commit -m "chore(release): date v0.3.0 changelog section"` then `git push origin main`. Watch the ci.yml run on main to ensure no last-mile regression: `gh run watch`. Tag and push: `git tag -a v0.3.0 -m "v0.3.0: Stabilization milestone"` then `git push origin v0.3.0`. Watch release.yml: `gh run watch` (this will take ~30-45 minutes for the 3-OS matrix build + bundle + sign + checksum publish). After completion, promote the draft to non-draft per Anti-pattern in 06-RESEARCH: `gh release edit v0.3.0 --draft=false`. Verify the release: `gh release view v0.3.0 --json tagName,isDraft,assets,body --jq '{tag:.tagName,draft:.isDraft,assetCount:(.assets|length),bodyLen:(.body|length)}'` — assetCount should be at least 8 (DMG + MSI + AppImage + .deb + SHA256SUMS-*.txt for each of 3 matrix legs + backend tar.gz from 06-03 + SHA256SUMS-backend-*.txt) and body should be non-empty (the CHANGELOG section was extracted by release.yml:322 awk parser). If body is empty or contains the "Auto-generated release for" fallback boilerplate, STOP — that means the awk parser did not match `## [0.3.0]` (Pitfall 1) and the release needs manual body fix via `gh release edit v0.3.0 --notes-file <(awk '/^## \[0\.3\.0\]/,/^## \[/' CHANGELOG.md | head -n -1)`.</action>
  <verify>
    <automated>RELEASE_GATE=1 uv run pytest tests/test_changelog.py -v &amp;&amp; gh release view v0.3.0 --json tagName,isDraft,isPrerelease,assets,body --jq '{tag:.tagName,draft:.isDraft,pre:.isPrerelease,assetCount:(.assets|length),bodyLen:(.body|length),hasFallback:(.body|test("Auto-generated release"))}'</automated>
  </verify>
  <done>CHANGELOG.md `## [0.3.0]` is dated. `RELEASE_GATE=1` pytest passes. Tag `v0.3.0` exists. GH Release is published non-draft with assetCount >= 8 and bodyLen > 1000 (the CHANGELOG section). No "Auto-generated release for" fallback in body.</done>
</task>

<task type="auto">
  <name>Task 5: Populate verification grid + matrix + file 3 deferral tracking issues</name>
  <files>CHANGELOG.md, .planning/release/verification-matrix.md</files>
  <action>FIRST, query the closure data: `gh issue list --milestone v0.3.0 --state closed --limit 100 --json number,title,closedAt,closedByPullRequestsReferences,labels > .planning/retros/v0.3.0-closure-data.json`. For each of the 13 originally-open issues (35, 42, 48, 54, 55, 56, 57, 58, 60, 65, 72, 76, 80): (1) read the issue state via `gh issue view <n> --json state,closedByPullRequestsReferences`; (2) determine end state — Fixed (closed by code PR), Documented workaround (closed by docs PR + has the workaround surfaced in error UI per Phase 1 INST-03/INST-04), Deferred-to-v0.4 (still open with v0.4 milestone), Closed-without-code-change (closed without a PR — e.g., empty bug report, no repro); (3) fill the corresponding row in both `CHANGELOG.md` Verification grid AND `.planning/release/verification-matrix.md` with the actual PR number, the verifier (which clean-VM exercise / which regression test), and the end-state column. Use Edit tool per row to avoid blast-radius mistakes. Once all 13 rows are filled, commit: `git add CHANGELOG.md .planning/release/verification-matrix.md && git commit -m "chore(release): populate v0.3.0 verification grid + matrix"` and push. Then file the 3 deferral tracking issues per `<interfaces>` templates. Create the v0.4 milestone first if missing: `gh api -X POST /repos/debpalash/OmniVoice-Studio/milestones -f title=v0.4 -f state=open --silent 2>/dev/null || true`. Then create the three issues using the exact templates from 06-RESEARCH "Deferral tracking issue template" — each with label `v0.4,deferred-from-v0.3` and milestone `v0.4`. Capture the three new issue numbers. Edit CHANGELOG.md Deferred section + verification-matrix.md to replace placeholder `#XXX` / `#YYY` / `#ZZZ` references with the actual issue numbers. Final commit: `git commit -am "chore(release): file v0.4 deferral tracking issues"` and push. Finally, confirm REL-06: every one of the 13 originally-open issues is in a confirmed end state. Run `gh issue view <n> --json state,labels` for each of 35/42/48/54/55/56/57/58/60/65/72/76/80 — every issue must be `state: CLOSED` OR `state: OPEN` with milestone `v0.4`. Document the per-issue end states in the verification-matrix.md "Wave-2 verifier signoff" column.</action>
  <verify>
    <automated>gh issue list --label deferred-from-v0.3 --milestone v0.4 --state open --json number --jq 'length' &amp;&amp; awk '/^## \[0\.3\.0\]/{flag=1;next}/^## \[/{flag=0}flag' CHANGELOG.md | grep -cE "^\| #(35|42|48|54|55|56|57|58|60|65|72|76|80) " &amp;&amp; for n in 35 42 48 54 55 56 57 58 60 65 72 76 80; do echo -n "$n:"; gh issue view $n --json state --jq .state; done</automated>
  </verify>
  <done>3 tracking issues exist with label `deferred-from-v0.3` on milestone `v0.4`. CHANGELOG.md Verification grid has 13 rows matching the originally-open issue numbers. Every one of those 13 issues is either CLOSED or in the v0.4 milestone. verification-matrix.md "Wave-2 verifier signoff" column is filled for all rows.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 6: Discord support-volume baseline — quantitative or qualitative?</name>
  <decision>Whether to spend the time on a quantitative Discord support-volume baseline for REL-04 metric (c), or accept a qualitative delta per Pitfall 6 in 06-RESEARCH.</decision>
  <context>
    REL-04 requires three metrics in the retro. Metric (a) and (b) come from `gh issue list` and are mechanical. Metric (c), Discord support-volume delta on install / HF-token / dubbing topics, requires either: (i) Discord admin history export + topic-tagging of the last 7 days pre-rc1 AND first 7 days post-v0.3.0 (~2-4 hours of work, requires admin access); or (ii) a qualitative directional finding ("install support volume noticeably down; HF-token down; dubbing volume unchanged"), which respects REL-04 wording but is less crisp.
    Per Pitfall 6 in 06-RESEARCH: "If even that's not feasible, document the metric as qualitative, not quantitative — Discord support-volume delta direction (up/down) only — and accept that REL-04 reports a directional finding, not a precise number."
    Scope decision 5 in the planning context says release notes auto-generate from milestone PRs + council synthesis — implies the maintainer is operating in time-conservative mode. The qualitative path keeps the retro shippable today rather than blocking on Discord scraping.
  </context>
  <options>
    <option id="quantitative">
      <name>Quantitative: scrape Discord, tag topics, compute delta</name>
      <pros>Crisp number; satisfies REL-04 literally; great signal for v0.4 planning.</pros>
      <cons>2-4 hours of manual work; requires admin access (ASSUMED unavailable per 06-RESEARCH Open Question 3); delays retro by a day.</cons>
    </option>
    <option id="qualitative">
      <name>Qualitative: directional delta only (down / unchanged / up per topic), documented as such</name>
      <pros>Retro ships today; honors REL-04's "delta on top 3 topics" without overcommitting; future releases can establish a baseline going forward; aligns with Pitfall 6 fallback in 06-RESEARCH.</pros>
      <cons>Less crisp; not directly comparable to future releases unless we keep the qualitative format.</cons>
    </option>
  </options>
  <files>.planning/retros/v0.3.0-retro.md</files>
  <action>Decision checkpoint — pause execution and present the two options above. No automated action; the executor MUST stop and surface the resume-signal prompt. The selected option determines how Task 7's metric (c) is filled in.</action>
  <verify>
    <human-check>User selects either "quantitative" or "qualitative". The choice is recorded in the retro doc's metric (c) source/method line.</human-check>
  </verify>
  <done>User responds with one of the two option IDs. Task 7 reads the decision and authors metric (c) accordingly.</done>
  <resume-signal>Select: quantitative or qualitative.</resume-signal>
</task>

<task type="auto">
  <name>Task 7: Publish v0.3.0 retrospective</name>
  <files>.planning/retros/v0.3.0-retro.md</files>
  <action>Open `.planning/retros/v0.3.0-retro.md` (the stub from 06-01 Task 4). Fill it in per the structure already there: (1) Header — link to milestone URL, release URL `https://github.com/debpalash/OmniVoice-Studio/releases/tag/v0.3.0`, rc1 + v0.3.0 tag dates. (2) ## Metrics — three sub-sections. Metric (a) Weighted closure count: read `.planning/retros/v0.3.0-closure-data.json` from Task 5; compute a weighted count per the REL-04 weighting rule (use a simple weight: Fixed = 1.0, Documented workaround = 0.7, Closed-without-code-change = 0.3; record the weighting rule in the retro so future releases can compare). Metric (b) Net inbox change: read `.planning/retros/v0.3.0-net-inbox.json` from Task 5 (re-run the `gh issue list --search "is:issue created:>=$MILESTONE_START"` query if needed); count closed minus opened during 2026-05-16 -> v0.3.0 tag date; record the number. Metric (c): per the resolution of Task 6's decision: if quantitative, do the Discord scraping and record numbers; if qualitative, record directional findings per topic (install / HF-token / dubbing) and explicitly note this is qualitative per the Task 6 decision. (3) ## What Worked — 4-VM exercise caught X; the workflow-parity job from 06-02 caught Y in PR Z; the backend-split + mirror-cascade integration in 06-03 worked; opt-in bug reporting (Phase 5) generated N reports during soak; ... (synthesize 5-10 items from Phase 0-5 SUMMARYs and the scope-decision rationale; do not fabricate — only items that are objectively true). (4) ## What To Change — build-time version templater is captured as v0.4 deferred improvement (per 06-01 Task 4's `.planning/decisions/pr-73-strategy.md`); the PR #73 rebase-vs-reimplement decision shows the cost of letting a branch go 93 commits stale (cadence lesson); Discord baseline gap is documented for v0.4 (start the baseline NOW going forward); ... (5) ## Cross-AI / Council notes — if any cross-AI review feedback is in `.planning/` synthesize it; if none, write "No external review for v0.3.0; v0.4 should consider opening rc1 for cross-AI review per the council pattern". (6) ## Closure verification — link to `.planning/release/verification-matrix.md`; state explicitly that all 11 (now 13) originally-open issues are in a confirmed end state per REL-06. Commit: `git add .planning/retros/v0.3.0-retro.md .planning/retros/v0.3.0-closure-data.json .planning/retros/v0.3.0-net-inbox.json && git commit -m "docs(retro): publish v0.3.0 retrospective" && git push origin main`.</action>
  <verify>
    <automated>test -f .planning/retros/v0.3.0-retro.md &amp;&amp; wc -l .planning/retros/v0.3.0-retro.md &amp;&amp; grep -c "^## " .planning/retros/v0.3.0-retro.md &amp;&amp; grep -cE "Weighted closure|Net inbox|Discord support" .planning/retros/v0.3.0-retro.md</automated>
  </verify>
  <done>Retro doc has 6 top-level sections (Header is sub-headers, plus ## Metrics, ## What Worked, ## What To Change, ## Cross-AI / Council notes, ## Closure verification). All 3 REL-04 metrics are present and have a Result field (numeric or directional). Closure verification section links to verification-matrix.md and confirms REL-06. Committed and pushed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local main branch to GH tag push | An unintended commit on main would be tagged and shipped — pre-tag gates defend |
| rc1 draft release to clean-VM downloads | SHA-256 verification on the maintainer's end before installing on each VM |
| 48-hour soak monitoring channels | Discord + GH Issues + auto-report inbox — missed reports promote a buggy rc |
| `gh release edit --draft=false` | Idempotent but irreversible (public Release; rolling back means yanking + reuploading); confirm body is correct before promoting |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06.04-01 | Tampering | Tag pushed before CHANGELOG dated (Pitfall 1) | mitigate | Task 4 runs `RELEASE_GATE=1 pytest tests/test_changelog.py` as the date-gate BEFORE the v0.3.0 tag push |
| T-06.04-02 | Tampering | Rc tag force-recreated (Pitfall 7) | mitigate | Treat every tag as immutable; if rc1 has a typo, cut rc2 — do NOT delete and re-push the same name |
| T-06.04-03 | Repudiation | Soak monitoring missed a weekend regression report (Pitfall 5) | mitigate | Task 3 cadence is 2x/day in working hours, soak start time chosen to keep the window inside working hours |
| T-06.04-04 | Denial of Service | release.yml fails mid-publish, partial assets attached | mitigate | `gh release view v0.3.0 --json assets` post-publish check; if asset count < expected 8, re-run via `gh workflow run release.yml --ref v0.3.0` or fall through to a manual `softprops/action-gh-release` upload |
| T-06.04-05 | Information Disclosure | Retro contains scraped Discord content with usernames | accept | Retro records aggregated counts and topic categories only; no raw message text; no usernames |
| T-06.04-06 | Tampering | Notarization scope creep during soak (Pitfall 4) | mitigate | Scope decision 1 locks notarization as v0.4; the deferral tracking issue filed in Task 5 absorbs any pressure during the soak — "your concern is captured, it will land in v0.4" |
| T-06.04-SC | Tampering | Supply chain — no package installs in this plan | n/a | Plan 06-04 is operational, not infrastructural. Zero npm/pip/cargo adds. |
</threat_model>

<verification>
- `gh release view v0.3.0 --json tagName,isDraft,isPrerelease,assets --jq '{tag:.tagName,draft:.isDraft,pre:.isPrerelease,assetCount:(.assets|length)}'` => `{tag:"v0.3.0", draft:false, pre:false, assetCount: >= 8}`
- `RELEASE_GATE=1 uv run pytest tests/test_changelog.py -v` => pass
- `grep -E "^## \[0\.3\.0\] — 2[0-9]{3}-[0-9]{2}-[0-9]{2}$" CHANGELOG.md` => 1 match
- `gh issue list --label deferred-from-v0.3 --milestone v0.4 --state open --json number --jq 'length'` => 3
- For each of `35 42 48 54 55 56 57 58 60 65 72 76 80`: `gh issue view <n> --json state,milestone` shows CLOSED or v0.4-milestone
- `.planning/retros/v0.3.0-retro.md` exists with all 6 sections and 3 metrics populated
- `.planning/release/verification-matrix.md` "Wave-2 verifier signoff" column is filled for all rows
</verification>

<success_criteria>
- v0.3.0 is published as a non-draft GitHub Release with installers + SHA-256 checksums + backend tar.gz per platform
- CHANGELOG.md `## [0.3.0]` section is dated and the verification grid points each originally-open issue to a real PR/commit/docs change
- 48-hour soak completed with no regression OR cut rc2 if soak hit a regression (and re-ran Tasks 1-3 against rc2)
- 3 deferral tracking issues exist on the v0.4 milestone
- Retrospective is published with 3 metrics (per Task 6 decision)
- All 11 (now 13) originally-open issues are in a confirmed end state (Closed via fix / Closed via documented workaround + UI / Deferred to v0.4)
- REL-01 through REL-06 are all satisfied
</success_criteria>

<output>
Create `.planning/phases/06-release-verification-retro/06-04-SUMMARY.md` when done. This is the FINAL plan of the FINAL phase — the SUMMARY should also note the v0.3.0 milestone completion for the cross-milestone retrospective trail.
</output>
