---
phase: 05-opt-in-bug-reporting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/services/redactor.py
  - backend/services/bug_report.py
  - backend/services/settings_store.py
  - backend/api/routers/bug_report.py
  - backend/main.py
  - tests/backend/services/test_redactor.py
  - tests/backend/services/test_bug_report.py
  - tests/backend/api/test_bug_report_router.py
autonomous: true
requirements:
  - REPORT-01
  - REPORT-02
  - REPORT-03
  - REPORT-05
  - REPORT-06
  - REPORT-07
  - REPORT-08
  - REPORT-10
  - REPORT-11
requirements_split:
  REPORT-01: "Python producer half (last-error channel + global_exception_handler extension + subprocess-errors reader) in this plan. Rust producer (panic hook) + React producer wiring (installConsoleCapture call site) shipped in Plan 05-02."
  REPORT-12: "Default-OFF semantic enforced by settings_store default value here (bug_reporting.enabled=\"0\"); UI toggle + opt-in copy shipped in Plan 05-02."

must_haves:
  truths:
    - "redact() is idempotent: redact(redact(s)[0]) == redact(s) for any input string"
    - "redact() strips HF tokens (hf_...), OpenAI keys (sk-...), email-like patterns, and rewrites $HOME paths (/Users/x/, /home/x/, C:\\Users\\x\\) to ~/"
    - "BugReporter.collect() reads ONLY fields in ALLOWED_FIELDS — no os.environ, no audio file contents, no HF token value (only get_token() truthiness)"
    - "BugReporter.report() guarded by threading.local recursion flag cleared in finally"
    - "Identical payload submitted twice in one session returns {ok:false, reason:'duplicate-this-session'} on second call (SHA-1 dedup)"
    - "Fourth report attempt within 24h returns {ok:false, reason:'rate-cap'} (default cap=3)"
    - "build_issue_url returns a URL with shape github.com/<owner>/<repo>/issues/new?title=&body=&labels=auto-report and is <= 6 KB UTF-8-encoded; oversize payloads spill to $OMNIVOICE_DATA/bug-reports/<sha1>.md with body replaced by a 'see attached log' pointer"
    - "global_exception_handler in backend/main.py writes the last Python error (class, message, stack) into the in-memory last-error channel before returning the response — without mutating the response shape"
    - "When $OMNIVOICE_DATA/subprocess-errors/<engine>-<pid>.json files exist, BugReporter merges the most-recent 5 into the payload as a 'Subprocess errors' section; when the directory does not exist, the section is omitted silently"
    - "POST /api/bug-report/preview returns {ok, body, search_url, issue_url, sha1, redactions_applied[]} on success and {ok:false, reason} on guard hits"
    - "settings_store.get_text('bug_reporting.enabled', '0') returns '0' on a fresh install (default-OFF — Plan 05-02 wires the UI toggle)"
  artifacts:
    - path: "backend/services/redactor.py"
      provides: "redact(text) and filter_env(env) — deterministic regex + string-replace; pure function, importable without side effects"
      contains: "def redact"
      min_lines: 60
    - path: "backend/services/bug_report.py"
      provides: "BugReporter singleton: collect(), redact, sha1 dedup, rate cap, recursion guard, render_body(), build_issue_url(), build_search_url(), spill-to-disk, subprocess-errors reader, last-error channel set/get"
      contains: "class BugReporter"
      min_lines: 250
    - path: "backend/services/settings_store.py"
      provides: "Extend Phase 1 store with bug_reporting.enabled key (text '0'/'1', default '0'). If file does not yet exist (Phase 1 plan dep not merged), create it with get_text/set_text scaffolding matching the AUTH-02 schema."
      contains: "bug_reporting"
    - path: "backend/api/routers/bug_report.py"
      provides: "FastAPI router exposing POST /api/bug-report/preview, POST /api/bug-report/build-url, GET/PUT /api/settings/bug-reporting/state. Reuses the Phase 1 loopback Host header guard pattern from backend/api/routers/settings.py (mirror that helper or import)."
      contains: "router = APIRouter"
      min_lines: 100
    - path: "backend/main.py"
      provides: "Extension of existing global_exception_handler (line 347) to push {source:'python', class, message, stack, ts} into bug_report.LAST_ERROR_CHANNEL before existing response logic; existing response shape unchanged"
      contains: "LAST_ERROR_CHANNEL"
    - path: "tests/backend/services/test_redactor.py"
      provides: "Unit tests: HF token redaction, OpenAI key redaction, email redaction, $HOME path rewriting on macOS/Linux/Windows shapes, env-name filter, idempotency"
      min_lines: 80
    - path: "tests/backend/services/test_bug_report.py"
      provides: "Unit tests: allow-list scope, URL shape + labels=auto-report, 6KB cap + spill, dedup, rate cap, recursion guard, last-error channel, subprocess-errors reader"
      min_lines: 150
    - path: "tests/backend/api/test_bug_report_router.py"
      provides: "Integration tests against FastAPI TestClient: /preview success path, /preview rate-cap path, /preview dedup path, /state default-OFF, /state PUT loopback guard returns 403 from non-loopback Host"
      min_lines: 80
  key_links:
    - from: "backend/services/bug_report.py"
      to: "backend/core/links.py (Phase 1 Wave 2 Task 2)"
      via: "PROJECT_REPO_URL constant for github.com/<owner>/<repo>"
      pattern: "from backend\\.core\\.links import PROJECT_REPO_URL"
    - from: "backend/services/bug_report.py"
      to: "backend/services/redactor.py"
      via: "redact() called per-field at collection time AND once on final body string (Pitfall #3)"
      pattern: "redactor\\.redact"
    - from: "backend/services/bug_report.py"
      to: "$OMNIVOICE_DATA/bug-reports/state.json"
      via: "Rate-cap window state read/written on every report attempt"
      pattern: "bug-reports.*state\\.json"
    - from: "backend/services/bug_report.py"
      to: "$OMNIVOICE_DATA/subprocess-errors/*.json"
      via: "Glob most-recent 5 child-side excepthook outputs (Phase 2 handoff)"
      pattern: "subprocess-errors"
    - from: "backend/main.py global_exception_handler"
      to: "backend/services/bug_report.LAST_ERROR_CHANNEL"
      via: "set() called before existing response logic (must not change existing response)"
      pattern: "LAST_ERROR_CHANNEL\\.set"
    - from: "backend/api/routers/bug_report.py PUT /api/settings/bug-reporting/state"
      to: "loopback Host header guard"
      via: "Mirror Phase 1 settings router T-02-04 helper — non-loopback Host returns 403"
      pattern: "loopback|127\\.0\\.0\\.1"

threat_model:
  - id: T-05-01
    category: Information Disclosure
    component: backend/services/redactor.py
    disposition: mitigate
    mitigation: "Redactor runs at field-collection boundary AND on final body (idempotent — test asserts redact(redact(x)[0]) == redact(x)). Pitfall #3 explicitly handled. Regression test fixture covers $HOME paths, hf_/sk- tokens, email-like patterns, env names matching *TOKEN*|*KEY*|*SECRET*."
  - id: T-05-02
    category: Information Disclosure
    component: backend/services/bug_report.py ALLOWED_FIELDS
    disposition: mitigate
    mitigation: "Default-deny allow-list (REPORT-03): collector reads ONLY explicitly enumerated lambdas. No `os.environ` read into payload; no audio file content read; HF token presence captured as bool only via `huggingface_hub.get_token()` truthiness, never the value. Unit test asserts no field outside ALLOWED_FIELDS appears in collect() output."
  - id: T-05-03
    category: Tampering
    component: backend/services/bug_report.py build_issue_url
    disposition: mitigate
    mitigation: "All user-content goes through urllib.parse.urlencode(params, quote_via=quote). PROJECT_REPO_URL is hardcoded host/path from links.py (Phase 1). User input does not influence the host. Unit test asserts the encoded URL round-trips and starts with the pinned PROJECT_REPO_URL."
  - id: T-05-04
    category: Denial of Service
    component: backend/services/bug_report.py BugReporter.report
    disposition: mitigate
    mitigation: "Three layered safeguards: threading.local recursion guard with try/finally reset (Pitfall #4), SHA-1 session dedup, daily rate cap (default 3 / 24h) persisted to $OMNIVOICE_DATA/bug-reports/state.json. Each safeguard has a dedicated unit test."
  - id: T-05-05
    category: Spoofing
    component: backend/api/routers/bug_report.py
    disposition: mitigate
    mitigation: "PUT /api/settings/bug-reporting/state reuses Phase 1's loopback Host header guard (T-02-04 pattern). Non-loopback Host returns 403. Test exercises the 403 path with a forged Host header."
  - id: T-05-06
    category: Information Disclosure
    component: $OMNIVOICE_DATA/bug-reports/<sha1>.md spill file
    disposition: mitigate
    mitigation: "Spill file is written ONLY after the same redactor pipeline that built the in-memory body — the spill file content is post-redaction. File mode is 0644 (readable by owner); directory parent created with default umask. Spill path is shown to the user in the dialog (Plan 05-02) — user sees what they are about to attach."
  - id: T-05-SC
    category: Tampering
    component: pip/uv installs in this plan
    disposition: mitigate
    mitigation: "No new Python packages. All deps (psutil, platform, urllib.parse, hashlib, tomllib, huggingface_hub, torch.cuda) are already pinned in pyproject.toml and audited as Approved in 05-RESEARCH.md Package Legitimacy Audit. No installs run; no slopcheck checkpoint required."
---

<objective>
Ship the backend half of opt-in bug reporting: the redactor, the BugReporter (collector + URL builder + rate-cap + dedup + recursion guard + 6 KB spill + subprocess-errors reader), the FastAPI router that exposes /preview /build-url /state, the global_exception_handler extension that publishes Python errors into the last-error channel, and the settings_store key that defaults bug_reporting.enabled to "0".

Purpose: 100% of the privacy-critical logic (default-deny allow-list, redaction, safeguards) lives in pure-Python modules that are 100% unit-testable without UI. By landing this wave first, downstream waves only consume an already-tested HTTP surface — no UI dependency on un-redacted data ever exists, even transiently. This plan is the bedrock of REPORT-03/05/06/07/08/10/11; downstream plans assume these are already enforced.

Output: redactor.py, bug_report.py, bug_report router, settings_store extension, global_exception_handler patch, three test files. No frontend touched, no Rust touched, no UI surface yet.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-opt-in-bug-reporting/05-RESEARCH.md

@CLAUDE.md
@pyproject.toml
@backend/main.py

<interfaces>
<!-- Phase 1 dependency: backend/core/links.py is shipped by Phase 1 Plan 01-02 Task 2 -->
<!-- Phase 5 Plan 01 imports PROJECT_REPO_URL — do not re-implement repo resolution -->

```python
# From backend/core/links.py (Phase 1) — single source of truth
PROJECT_REPO_URL: str   # e.g. "https://github.com/debpalash/OmniVoice-Studio"
```

<!-- Phase 1 dependency: backend/services/settings_store.py is shipped by Phase 1 Plan 01-01 (AUTH-02) -->
<!-- This plan EXTENDS that store with one additional text key. If 01-01 has not merged when this plan executes, create settings_store.py with the AUTH-02 schema (SQLite settings table) as a scaffolding step BEFORE the bug_reporting.enabled key — but note this is a fallback; coordinator should land 01-01 first. -->

```python
# Phase 1 contract — re-used here
settings_store.get_text(key: str, default: str = "") -> str
settings_store.set_text(key: str, value: str) -> None
```

<!-- New surface this plan EXPORTS for downstream Plan 05-02 to consume -->

```
POST /api/bug-report/preview
  body: { source: "error-boundary"|"settings"|"rust-panic"|"subprocess", errorClass?: str, errorMessage?: str, errorStack?: str }
  → 200 { ok: true,  body: str, search_url: str, issue_url: str, sha1: str, redactions_applied: list[str], truncated: bool, local_log_path?: str }
  → 200 { ok: false, reason: "recursion-guard"|"duplicate-this-session"|"rate-cap", sha1?: str, next_allowed_at?: float }

GET  /api/settings/bug-reporting/state            → { enabled: bool }
PUT  /api/settings/bug-reporting/state {enabled}  → { enabled: bool }    (loopback Host only — 403 otherwise)
```

```python
# Importable singleton + module-level channel (Plan 05-02 Rust panic hook writes into it via FFI? No — Rust writes to disk; Python reads disk. React side calls /preview.)
from backend.services.bug_report import BugReporter, LAST_ERROR_CHANNEL
LAST_ERROR_CHANNEL.set(payload: dict) -> None   # called from global_exception_handler
LAST_ERROR_CHANNEL.get() -> dict | None         # called from BugReporter.collect()
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Redactor (redactor.py + tests)</name>
  <files>backend/services/redactor.py, tests/backend/services/test_redactor.py</files>
  <behavior>
    - HF token `hf_` followed by >=20 alnum chars → `hf_***REDACTED***` (case-sensitive; verified per CLAUDE.md token shape)
    - OpenAI key `sk-` followed by >=20 alnum chars → `sk-***REDACTED***`
    - Email-like pattern `[\w.+-]+@[\w-]+\.[\w.-]+` → `***@***.***` (conservative regex — false positives acceptable; false negatives are the failure mode we are preventing)
    - `$HOME` paths: regex compiled at import time from Path.home() + literal fallbacks (`/Users/<name>/`, `/home/<name>/`, `C:\Users\<name>\` — handle both `/` and `\` separators on Windows) → `~/`
    - `filter_env(env)` drops any key matching `.*(TOKEN|KEY|SECRET).*` case-insensitive
    - `redact(text)` returns `(redacted_str, list[str])` where list enumerates which kinds were applied (so the dialog can show "We redacted: hf-token, home-paths-x3" for the user)
    - **Idempotency:** `redact(redact(x)[0])[0] == redact(x)[0]` for any x — explicit unit test
    - Empty / None / non-string input handled gracefully (return ("", []) for None)
    - Test cases include realistic stack-trace fixtures with embedded $HOME paths, embedded hf_ tokens, embedded emails
  </behavior>
  <action>Implement `backend/services/redactor.py` per the patterns documented in 05-RESEARCH.md "Pattern 3: Redaction Layer" (lines 330–368). Build the $HOME regex once at module import time from `Path.home()` plus literal fallbacks; on Windows escape backslashes correctly. Pure functions only — no I/O, no logging. Write `tests/backend/services/test_redactor.py` first (RED), then implement (GREEN). Tests MUST cover: each redaction kind in isolation, combined kinds in one string, idempotency, empty/None input, mixed-platform $HOME paths in the same string, and the env-name filter. Address Pitfall #3 explicitly: add a test where a multi-line stack contains both a hf_ token AND a $HOME path — assert both are redacted in the single call.</action>
  <verify>
    <automated>uv run pytest tests/backend/services/test_redactor.py -x -v</automated>
  </verify>
  <done>All test_redactor.py tests pass; idempotency test passes; no I/O in redactor.py (asserted by grep `^import\s*\(open\|requests\|httpx\|urllib\.request\)` returning zero non-comment matches against the module).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: BugReporter + URL builder + safeguards (bug_report.py + tests)</name>
  <files>backend/services/bug_report.py, tests/backend/services/test_bug_report.py</files>
  <behavior>
    - `ALLOWED_FIELDS` dict maps name → zero-arg lambda. `collect()` returns `{k: f() for k,f in ALLOWED_FIELDS.items()}` and nothing else. Default-deny — adding a new field requires editing the dict.
    - Fields collected (per CLAUDE.md Capability 2 + REPORT-03): os, python, machine, omnivoice_version (read via tomllib from pyproject.toml — reuse existing `backend/core/links.py` helper if present, else inline), backend_git_sha (try `git rev-parse HEAD` via subprocess with short timeout; on failure return "unknown"), cpu_count, ram_mb, gpu (CUDA name+VRAM via torch.cuda, MPS via torch.backends.mps.is_available, ROCm via torch.version.hip; fall back "cpu-only" if torch absent), active_engine, installed_engines (read from in-memory engine registry — wrap import in try/except so missing registry returns empty list)
    - HF token presence: `huggingface_hub.get_token()` is truthy → `hf_token_present: true`; never include the value
    - `LAST_ERROR_CHANNEL` is a singleton-like object: `.set(payload)` overwrites; `.get()` returns last or None; thread-safe (lock-protected dict swap)
    - `render_body(info, error_ctx)` renders the Markdown template exactly per 05-RESEARCH.md "Common Operation 2: Issue Body Markdown Template" (lines 627–666). All field interpolation goes through `redactor.redact()` BEFORE the f-string — never paste raw values into Markdown
    - `build_issue_url(title, body, labels) -> (url, truncated: bool)` uses `urllib.parse.urlencode(params, quote_via=urllib.parse.quote)`; URL must contain `labels=auto-report` (REPORT-10) — labels list comma-joined; truncated=True iff `len(url.encode("utf-8")) > 6 * 1024`
    - When truncated: write full body to `$OMNIVOICE_DATA/bug-reports/<sha1>.md` (mkdir parents OK), then `build_truncated_url(...)` replaces the body's "## Logs" / "## Last error" section (whichever is the heaviest in the rendered output) with the "see attached log" pointer to the absolute spill path. If still over budget after that, drop the body to "See `<path>`." Last-resort
    - `build_search_url(error_class, first_line)` uses `quote_plus` (NOT urlencode/quote) to preserve `:` in `is:issue` — Pitfall #5 explicit handling. Test fixture asserts `is%3Aissue` does NOT appear in output; `is:issue` does
    - `BugReporter.report(ctx) -> dict`:
        1. **Guard 1 — Recursion:** check `_recursion.in_progress` (threading.local). If set, return `{ok:false, reason:"recursion-guard"}`. Set on enter; clear in `finally`. Use `try/finally`, not `try/except/finally` — exceptions inside report must NOT be swallowed; they propagate to the caller AFTER the flag is cleared
        2. Build payload: assemble system info, attach error_ctx if present (read from LAST_ERROR_CHANNEL when ctx is None and source is "error-boundary"; read subprocess-errors directory always; cap at 5 most recent), redact each field, then redact the final rendered body
        3. **Guard 2 — Session dedup:** `hashlib.sha1(body.encode()).hexdigest()`; if in `_session_dedup`, return `{ok:false, reason:"duplicate-this-session", sha1}`
        4. **Guard 3 — Rate cap:** load `$OMNIVOICE_DATA/bug-reports/state.json`, filter entries with `time.time() - ts < 86400`. If `len(recent) >= 3`, return `{ok:false, reason:"rate-cap", next_allowed_at: recent[0]["ts"]+86400}`. Default cap is hardcoded constant DEFAULT_DAILY_CAP=3; expose `DEFAULT_DAILY_CAP` as a module constant so future config can override it
        5. On success: append `{sha1, ts}` to window, write state.json atomically (write to .tmp then rename), add sha1 to `_session_dedup`, build URL (cap+spill if needed), return `{ok:true, body, search_url, issue_url, sha1, redactions_applied, truncated, local_log_path?}`
    - `_read_subprocess_errors()` globs `$OMNIVOICE_DATA/subprocess-errors/*.json`, sorts by mtime desc, takes 5, loads each safely (try/except per file — corrupt file does NOT fail the whole report), returns list of dicts. Missing directory → []
    - Test cases (drive RED→GREEN):
        - `test_allow_list`: assert `set(collect().keys()) <= ALLOWED_FIELDS.keys()` AND no extra keys
        - `test_url_shape`: assert URL starts with `https://github.com/...` (whatever PROJECT_REPO_URL resolves to in test fixture), contains `?title=`, `&body=`, `&labels=auto-report`
        - `test_label`: assert `labels=auto-report` present (REPORT-10)
        - `test_url_length_cap`: oversized body → truncated=True, spill file written, returned URL <= 6 KB; second-call idempotency
        - `test_dedup`: same context twice → first ok=true, second ok=false reason=duplicate-this-session, same sha1
        - `test_rate_cap`: 3 distinct reports succeed, 4th returns reason=rate-cap with next_allowed_at set
        - `test_recursion_guard`: monkeypatch collect() to call `bug_reporter.report({})` from inside itself — outer ok=true, inner returns reason=recursion-guard, AND flag is False after outer returns (test asserts via reading `_recursion.in_progress`)
        - `test_recursion_guard_with_exception`: collect() raises; assert flag is reset to False AND exception propagates
        - `test_last_error_channel`: set() then get() returns payload; after second set(), get() returns latest
        - `test_subprocess_errors_reader_missing_dir`: returns []
        - `test_subprocess_errors_reader_corrupt_file`: writes valid + corrupt → reader returns just the valid one
        - `test_subprocess_errors_reader_caps_at_5`: writes 8 files → reader returns 5 most-recent
        - `test_search_url_preserves_dsl`: assert `is:issue` (not `is%3Aissue`) in output
        - `test_no_hf_token_value`: assert collect()["hf_token_present"] is bool AND no `hf_` literal anywhere in str(collect())
  </behavior>
  <action>Implement `backend/services/bug_report.py` per 05-RESEARCH.md "Architecture Patterns" Patterns 1–5, "Code Examples" Common Operations 1, 2, 5, and "Subprocess-Engine Error Capture" (lines 778–823). The `$OMNIVOICE_DATA` path resolution uses `os.environ.get("OMNIVOICE_DATA", "omnivoice_data")` — same convention as the rest of the codebase. Atomic state.json write: `Path.write_text` on `.tmp` then `Path.replace`. Module-level constants: `DEFAULT_DAILY_CAP = 3`, `URL_BUDGET = 6 * 1024`, `WINDOW_SEC = 86400`, `SUBPROCESS_ERRORS_LIMIT = 5`. Write `tests/backend/services/test_bug_report.py` first (RED — tests fail because module does not exist yet); then implement (GREEN). Address Pitfall #4 explicitly: the recursion-guard test that triggers an exception during collect() must assert the flag is reset. Address Pitfall #5 explicitly: the search-url test asserts `is:issue` is not over-encoded.</action>
  <verify>
    <automated>uv run pytest tests/backend/services/test_bug_report.py -x -v</automated>
  </verify>
  <done>All test_bug_report.py tests pass; module imports cleanly with no side effects (no I/O at import time — verified by `python -c "import backend.services.bug_report"` completing without writing to omnivoice_data/); ALLOWED_FIELDS keys match exactly what the rendered Markdown template references (a single failing test in test_bug_report.py asserts this — extra fields in the dict or missing fields in the template both fail the test).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Router + settings_store extension + global_exception_handler patch (+ tests)</name>
  <files>backend/api/routers/bug_report.py, backend/services/settings_store.py, backend/main.py, tests/backend/api/test_bug_report_router.py</files>
  <behavior>
    - `settings_store.py`: extend the AUTH-02 store (or create scaffolding if Plan 01-01 has not landed — see Action) to support a non-encrypted text key `bug_reporting.enabled`. Default value when unset is the string `"0"`. The Phase 1 schema uses an `encrypted` column; this key uses encrypted=0. If Plan 01-01 has merged, this is a one-line addition; if not, create the SQLite table per AUTH-02 schema (key TEXT PK, value TEXT, encrypted INT, updated_at INT) and the get_text/set_text helpers. NEVER raise on missing — return default
    - `backend/api/routers/bug_report.py`: APIRouter with three routes:
        - `POST /api/bug-report/preview` body {source, errorClass?, errorMessage?, errorStack?}: calls `BugReporter().report(ctx)`. If `settings_store.get_text("bug_reporting.enabled","0") != "1"` returns 403 (reporter is OFF). Otherwise returns the BugReporter dict as JSON
        - `GET /api/settings/bug-reporting/state` → `{enabled: bool}` (`settings_store.get_text("bug_reporting.enabled","0") == "1"`). No auth check needed (state is non-sensitive; GET is loopback-bound by FastAPI's default Host)
        - `PUT /api/settings/bug-reporting/state` body `{enabled: bool}`: BEFORE writing, check the request Host header is `127.0.0.1[:port]` or `localhost[:port]` — if not, return 403. Mirror the helper from `backend/api/routers/settings.py` (Phase 1 Plan 01-02 T-02-04). If the helper exists, import it; else inline a function `_assert_loopback(request)` that pulls `request.headers.get("host","")` and matches against `^(127\.0\.0\.1|localhost)(:\d+)?$`
    - `backend/main.py`: inside the existing `async def global_exception_handler(request, exc)` at line 347, AFTER the existing "client disconnect" early-return and BEFORE the existing serialize-to-crash-log block, add:
        ```python
        try:
            from backend.services.bug_report import LAST_ERROR_CHANNEL
            import traceback as _tb, time as _t
            LAST_ERROR_CHANNEL.set({
                "source": "python",
                "class": exc.__class__.__name__,
                "message": str(exc),
                "stack": "".join(_tb.format_tb(exc.__traceback__)) if exc.__traceback__ else "",
                "ts": _t.time(),
            })
        except Exception:
            # bug_report must never break the existing error handler
            pass
        ```
        Existing response logic is unchanged. The patch is additive and try-guarded — if bug_report module is unavailable, the existing handler still works.
    - Register the new router in the existing FastAPI app: add one line where other routers register (search `app.include_router` in main.py — typical pattern). Tag: `bug-report`.
    - Tests (test_bug_report_router.py) — use FastAPI TestClient + monkeypatched settings_store:
        - `test_state_default_off`: GET /state returns {enabled: false} on a fresh store
        - `test_state_put_toggles`: PUT {enabled: true} from loopback Host → 200 + enabled=true; subsequent GET returns enabled=true
        - `test_state_put_non_loopback_403`: PUT with Host header "evil.com" → 403
        - `test_preview_off_returns_403`: enabled=false → POST /preview returns 403 (reporter is off)
        - `test_preview_on_returns_payload`: enabled=true → POST /preview returns {ok:true, body, issue_url, ...}
        - `test_preview_rate_cap`: enabled=true; 3 successful posts then 4th → ok=false reason=rate-cap
  </behavior>
  <action>Three coordinated edits with explicit ordering: (a) extend `settings_store.py` (create if missing per Phase 1 AUTH-02 schema); (b) create router file and register in main.py; (c) patch `global_exception_handler` with try-guarded LAST_ERROR_CHANNEL.set. The router file MUST import `BugReporter` and `LAST_ERROR_CHANNEL` from `backend.services.bug_report` and `get_text/set_text` from `backend.services.settings_store`. Write `tests/backend/api/test_bug_report_router.py` first (RED — endpoints do not exist); then implement (GREEN). The loopback-guard test MUST use `client.put(..., headers={"Host": "evil.com"})` to actually exercise the path; mocking the helper does not count.</action>
  <verify>
    <automated>uv run pytest tests/backend/api/test_bug_report_router.py tests/backend/services/test_bug_report.py tests/backend/services/test_redactor.py -x -v</automated>
  </verify>
  <done>All four test files pass; `uv run pytest -x` returns green for the full backend suite (no regressions); the global_exception_handler patch is verified by an integration test that raises a synthetic 500 and asserts `LAST_ERROR_CHANNEL.get()` returns the captured class+message+stack; `grep -n "include_router.*bug_report" backend/main.py` shows the router is wired.</done>
</task>

</tasks>

<verification>
- `uv run pytest tests/backend/services/test_redactor.py tests/backend/services/test_bug_report.py tests/backend/api/test_bug_report_router.py -x -v` — all green
- `uv run pytest -x` — full backend suite green (no regression in existing endpoints or main.py)
- Manual: `curl -s -X POST http://127.0.0.1:8000/api/bug-report/preview -H "Content-Type: application/json" -d '{"source":"settings"}'` returns either 403 (off) or JSON with `ok`, `body`, `issue_url`, `sha1`. With `enabled=1` set via PUT, the response should include `labels=auto-report` in `issue_url` and the body should start with `## Description` per the rendered template.
- Grep gate: `grep -nv '^#' backend/services/redactor.py | grep -c 'open\|requests\|httpx\|urllib\.request'` returns 0 (redactor has zero I/O).
- Grep gate: `grep -nv '^#' backend/services/bug_report.py | grep -c 'os\.environ\[' ` returns 0 (no broad env read; only os.environ.get("OMNIVOICE_DATA", ...) is allowed — which is the get() form, not subscript).
</verification>

<success_criteria>
- All three test files pass; existing backend tests remain green.
- REPORT-02: build_issue_url returns a URL with the documented shape; unit test asserts labels=auto-report and host starts with PROJECT_REPO_URL.
- REPORT-03: collect() reads ONLY ALLOWED_FIELDS keys; unit test asserts the set equality.
- REPORT-05: redact() handles all listed patterns and is idempotent; unit tests cover each.
- REPORT-06: rate cap blocks the 4th report within 24h; unit test asserts.
- REPORT-07: SHA-1 dedup blocks identical second report in a session; unit test asserts.
- REPORT-08: Recursion guard prevents nested reporter calls and resets cleanly on exception; two unit tests assert.
- REPORT-10: `labels=auto-report` present in every generated URL.
- REPORT-11: URL > 6 KB encoded triggers spill to `$OMNIVOICE_DATA/bug-reports/<sha1>.md` and a truncated URL with "see attached log" pointer.
- REPORT-01 (Python half): `global_exception_handler` writes into `LAST_ERROR_CHANNEL`; subprocess-errors directory is read at report-build time.
- Plan 05-02 can consume `POST /api/bug-report/preview` and `GET/PUT /api/settings/bug-reporting/state` without further backend changes.
</success_criteria>

<output>
Create `.planning/phases/05-opt-in-bug-reporting/05-01-SUMMARY.md` when done with:
- File-by-file change list
- Test counts per file (e.g., test_redactor.py: 14 tests, test_bug_report.py: 13 tests, test_bug_report_router.py: 6 tests)
- Decisions made about ALLOWED_FIELDS final shape (which fields, source for each)
- Any deviation from the research spec, with rationale
- Open questions for Plan 05-02 (e.g., the exact ctx shape from React errorBoundary)
- Confirmation: `LAST_ERROR_CHANNEL`, `BugReporter`, `redact`, settings_store key `bug_reporting.enabled` are all importable/queryable.
</output>
